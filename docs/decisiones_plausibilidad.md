# Decisiones de plausibilidad ya cerradas

Log de hallazgos ya investigados y corregidos (o aceptados con documentación) — atajo de arranque
de sesión, ver `CLAUDE.md` para las restricciones de uso. **No repetir la investigación ni volver
a dudar de algo ya verificado aquí**; si hace falta el detalle cuantitativo completo, está en el
docstring de `motor/evolucion_arquetipo.py` (buscar la sección del arquetipo correspondiente) y en
los tests de regresión que lo bloquean.

| # | Hallazgo | Corrección / decisión | Test de regresión |
|---|---|---|---|
| 1 | Endeudamiento sin techo de plausibilidad sectorial | Chequeo incondicional cada año contra `huber+3·MAD`, techo absoluto 0,85 (bajado de 0,95 tras comprobar que 26/1.296 casos del arquetipo 9 se colaban con 0,95) | `test_apalancamiento_topa_la_deuda_nueva_al_techo_del_sector` |
| 2 | "Espiral de deuda" del arquetipo 9: el interés de deuda YA acumulada erosiona PN en años sin deuda nueva, sin quedar señalizado | El chequeo de endeudamiento se quitó de estar condicionado a `deuda_extra_por_nof_eur>0` — ahora corre siempre. Antes: 130/1.296 sin señalizar; después: 0 | `test_espiral_de_deuda_de_apalancamiento_siempre_queda_señalizada` |
| 3 | Techo propio de `consumos_explotacion` no acotaba el `margen_bruto` derivado (arquetipo 11) | `_limitar_por_subtotal`: topa el subtotal que alimenta la primitiva a su propio huber±3·MAD (`SUBTOTAL_PYG_DE_PRIMITIVA`). Antes: 1.796/2.592 (69%) sin señalizar; después: 0 | `test_margen_bruto_respeta_el_techo_de_plausibilidad_del_sector` |
| 4 | Dampear una masa de PASIVO (acreedores_comerciales) como palanca de contención tiene efecto neto CERO sobre endeudamiento | Palancas de contención restringidas a masas de ACTIVO (`SIGNO_NOF_MASA_CIRCULANTE == 1`) | Cubierto por los tests del arquetipo 4 en `test_arquetipos_generalizados.py` |
| 5 | Arquetipo 15: ¿tesorería puede quedar negativa/absurda sin señal? | No — el suelo genérico "disponible no puede ser negativo" del pipeline ya lo cubre; comprobado 0/6.480 casos | — (verificación puntual, no hubo fix) |
| 6 | Arquetipo 10 (baii, base dinámica): sin techo, `baii_pct` rebasaba el suyo sin señal | `_limitar_gastos_personal_por_baii`. Antes: 316/648 (48,8%) sin señalizar; después: 0 | `test_mejora_ebitda_baii_respeta_el_techo_de_plausibilidad_del_sector` |
| 7 | Arquetipo 10: la corrección del techo a veces invierte el sentido de `gastos_personal_pct` (sube en vez de bajar) | Se amortigua la intensidad del propio arquetipo antes de invertir el sentido; solo invierte cuando ni con intensidad cero bastaría (ruido de la base ese año). Señalizado con `pyg_contencion_al_limite`. 173/316 activaciones se resuelven sin invertir, 143/316 sí — las 143 coinciden EXACTAMENTE con la señal, 0 discrepancias en 648 casos | `test_mejora_ebitda_las_subidas_de_gastos_personal_siempre_coinciden_con_el_limite_por_ruido` |
| 8 | Sesgo de RNG compartido: `nota_memoria` del arquetipo 10 se sembraba con `rng_pyg`, que llega al punto del sorteo en el MISMO estado para cualquier sector/intensidad con la misma semilla | RNG dedicado `rngs_nota`, sembrado con hash estable (`zlib.crc32`) de sector+segmento+intensidad. Antes: reparto 12/37/56/33/5 en 143 casos (χ²≈58, p<0,001, NO es ruido de muestra); después: χ²=2,42 (uniforme) | Tests de `nota_memoria` en `test_arquetipos_generalizados.py` |
| 9 | Mismo sesgo, auditado y encontrado también en `generar_empresa_base` (año base 2023) y en `semilla_secuencia`/`rng_tendencia`/`rngs_pyg` (2024/2025, TODOS los arquetipos) | Mismo patrón de hash estable sector+segmento en ambos puntos — **deliberadamente sin mezclar intensidad** (mismo "ruido de fondo" entre intensidades del mismo caso, por diseño). Antes: z-scores idénticos hasta 1e-9 entre sectores distintos con la misma semilla (27/27 duplicados); después: 0 duplicados en 27×4 combinaciones. Forzó a re-pinnear los valores exactos del test de regresión del arquetipo 1 (el mecanismo no cambió, solo el ruido de fondo que recibe) | `test_sectores_distintos_no_comparten_ruido_pese_a_la_misma_semilla` (empresa_base) y `test_sectores_distintos_no_comparten_crecimiento_pleno_objetivo` (evolución) |
| 10 | Arquetipo 14 (ROE elevado por apalancamiento): ¿el ROE realmente sube al declararlo como variante exacta del 9? | El ROE *headline* solo sale más alto que la base 2023 en el 53,3% de 324 casos. Se probó (e implementó, midió y REVIRTIÓ) amortiguar el ruido operativo de las primitivas no forzadas — descartado: ni con ruido cero mejora (49,7%). Causa real: la misma "espiral de deuda" del hallazgo #2 aplicada al ROE — sube 61,7% en la transición 2024 vs. 2023 (el interés nuevo pesa "media dosis" ese año), pero solo 35,0% en 2025 vs. 2024 (interés ya pesa el año completo + contención limita la deuda nueva). En AL MENOS uno de los dos años: 78,1%, consistente por intensidad (77-79%). **Decisión: no se toca el motor** — el 78,1% ya es claramente mayoritario y tocar la rentabilidad operativa confundiría dos huellas distintas; la ficha docente es la que debe matizarse (pendiente, fuera del motor) para reflejar que el efecto se ve típicamente en el año en que se contrae la deuda, no necesariamente en el último año del caso | — (verificación cuantificada, sin fix en el motor) |
| 11 | Arquetipo 17 (capex): financiado con deuda a largo nueva, sin palanca de circulante para contener endeudamiento | Aceptado, mismo patrón ya establecido para 9/11/15/16: el chequeo incondicional señaliza siempre (0 casos sin señalizar en 972), pero el 100% de las activaciones (152/972, 15,6%) queda en `contencion_al_limite=True` — señal honesta sin corrección posible, no un intento fallido | `test_capex_elevado_riesgo_endeudamiento_siempre_señalizado` |
| 12 | Arquetipo 18 (adquisición): salto exógeno de activo financiado con caja+deuda ¿puede producir combinaciones no plausibles? | Mismo patrón que #11: el chequeo incondicional se activa en 70/972 (7,2%), el 100% señalizado, el 100% en `contencion_al_limite=True`. Además: en 3/324 casos (sector con `disponible` alto y `otras_deudas_corto` bajo de partida) el drenaje de caja de la operación hizo que el plug general de cuadre (ya existente, no nuevo) redirigiera un remanente hacia `disponible` — el balance sigue cuadrando exacto, es una interacción del mecanismo de cuadre PRE-EXISTENTE más visible aquí por la magnitud del salto, no un bug del arquetipo | `test_adquisicion_riesgo_endeudamiento_siempre_señalizado`, `test_adquisicion_incremento_de_activo_proporcional_a_intensidad` |
| 13 | Arquetipo 18 (adquisición): la sección 2.24 pide "fondo de comercio, si procede" como parte de la huella — el modelo no lo desglosa como partida propia | **Limitación conocida del modelo de balance actual, no resuelta ahora, no un olvido.** `MASAS_BALANCE` (motor/empresa_base.py) no tiene una categoría "fondo_comercio": el incremento de activo del arquetipo 18 se registra íntegro dentro de `activo_no_corriente`, sin distinguir qué parte sería inmovilizado material/intangible identificable y qué parte fondo de comercio. Añadirla exigiría tocar `MASAS_BALANCE`, la generación del balance base, y cada consumidor del balance (endeudamiento, ratios, memoria) — desproporcionado para lo que pide hoy la sección 2.24, que no exige una cifra de fondo de comercio separada, solo mencionarlo en la memoria "si procede" (ya cubierto por `NOTAS_MEMORIA_ADQUISICION`). Revisitar si una fase futura (p. ej. estados financieros con más desglose, o un arquetipo que necesite distinguir explícitamente el fondo de comercio, como pruebas de deterioro) lo exige | — (limitación documentada, sin test — no hay comportamiento que fijar) |
| 14 | Arquetipos 20/21 (`motor/memoria.py`): el % puro (sin suelo) de la magnitud de referencia podía dar importes citados ridículamente pequeños para una "operación relevante" de memoria | Verificado en pruebas de estrés (324 casos): arquetipo 20 (operaciones vinculadas) sin suelo — mínimo 8.000 €, 1 caso <10.000 €, 5 <20.000 €, 17 <50.000 € de 324; arquetipo 21 (coberturas) salió limpio (mínimo 60.000 €, 0 <50.000 €) pero se le añadió un suelo igualmente por el mismo criterio defensivo. Corregido con `SUELO_IMPORTE_VINCULADAS_EUR` (30.000 €) y `SUELO_NOCIONAL_COBERTURA_EUR` (50.000 €) — el `%` mostrado en el texto se RECALCULA sobre el importe ya topado, nunca el % originalmente sorteado, para que texto e importe nunca queden internamente inconsistentes. Verificado tras el fix: 0 casos por debajo del suelo Y 0 por encima del techo (activo total) en los mismos 324 casos, para ambos arquetipos — confirmado también que el suelo nunca puede empujar el importe por encima de la magnitud de referencia (deuda financiera mínima observada 151.500 €, muy por encima de cualquiera de los dos suelos) | `test_operaciones_vinculadas_importe_dentro_de_cotas_de_plausibilidad`, `test_coberturas_nocional_dentro_de_cotas_de_plausibilidad` |
| 15 | Arquetipo 22 (comodín, tema "dependencia de un proveedor clave") se solapa temáticamente con el arquetipo 7 (dependencia de pocos clientes) — mismo riesgo de concentración, lado contrario de la relación comercial | **RESUELTO — ver #16.** Cuando se combinen varios arquetipos de memoria sobre el mismo caso, evitar que un caso combine el tema "proveedores"+"concentracion" del 22 con el 7 ("clientes"+"concentracion") — no son contradictorios, pero sí repetitivos (dos riesgos de concentración a la vez, de lados opuestos de la cadena de valor). Marcar como "temáticamente solapados" en esa lógica futura, no aquí — las etiquetas ya existen (`concentracion` aparece en ambos) para que esa fase no tenga que rehacer el etiquetado | `test_arquetipos_7_y_22_evitan_colision_de_tema_en_barrido_amplio` (`tests/test_combinacion_arquetipos.py`) |
| 16 | Combinación de arquetipos (sección 2.12, acotada a las 6 combinaciones recomendadas de la sección 2.26): (a) ¿el motor soporta varios arquetipos a la vez? — no, antes de este hallazgo: `arquetipo_id` era un string único en toda la cadena; (b) al combinarlos, ¿se necesitan reglas nuevas de composición, y con qué coste en saturación? | (a) Se generalizó `generar_evolucion_arquetipo` a `generar_evolucion_combinada` (acepta `dict[arquetipo_id, intensidad]`); el caso de 1 arquetipo pasó a ser el wrapper de una línea, garantía estructural de que la regresión de los 21 arquetipos cerrados prueba la ausencia de cambio de comportamiento (confirmado: suite completa, 0 valores de referencia modificados). (b) De las 4 colisiones sospechadas al diseñar, 2 NO eran colisiones reales (Combo C "tesorería" y Combo D "BAII" ya componían bien por el pipeline existente, verificado leyendo código antes de asumir) — la única real fue Combo F (1+5) sobre `existencias`, resuelta sumando intensidades y aplicando la fórmula de continuidad una vez (ver "Combinación de arquetipos" en `CLAUDE.md`). **Hallazgo de saturación (verificación pedida explícitamente antes de dar el combo por bueno):** en barrido de referencia (27 sectores × 4 semillas, intensidad fuerte), arquetipo 1 solo activa `riesgo_endeudamiento` en 17,1% de los ejercicios, arquetipo 5 solo en 4,2% — la combinación 1+5 lo activa en 48,6% (muy por encima del ~20,6% esperable bajo independencia estadística de ambos efectos), y el Combo F completo (1+5+9+16) en 84,3%, con `contencion_al_limite` en el 71,4% de esas activaciones. Es la consecuencia directa y esperada de sumar intensidades sobre la misma variable de `masa_circulante` (regla aprobada) más el apalancamiento (9) y riesgo de refinanciación (16) actuando a la vez sobre la misma empresa ya tensionada — no un bug (balance sigue cuadrando en el 100% de los casos, la señalización nunca falla), pero sí una saturación desproporcionada frente a la suma ingenua de sus partes. **RESUELTO sin amortiguador de combinación — ver #20.** Investigación posterior: antes de tocar el Combo F, se descubrió que la mayor parte de esta "saturación" no venía de la combinación en sí, sino de que el arquetipo 9 en solitario ya activaba `riesgo_endeudamiento` en el 39,4%/67,6%/85,6% de los casos (leve/moderado/fuerte) — el Combo F completo (39,8%/67,6%/84,3%) era prácticamente idéntico a "9 solo". Tras recalibrar 9/10/11 (#20), el Combo F queda en 6,0%/22,2%/63,9% (leve/moderado/fuerte) — gradiente real, sin saturación en leve — confirmando la sospecha: **el amortiguador de combinación para 3+ arquetipos nunca hizo falta; la causa real era la calibración de intensidad de 9 en solitario, ya corregida** | `tests/test_combinacion_arquetipos.py` (17 tests: 6 combos × 2 sectores, disjunción exhaustiva de etiquetas 18/19/20/21, colisión 7+22 en barrido de 324 casos, fusión de `masa_circulante` sin descuadres, tasa de `contencion_al_limite` del Combo F registrada) |
| 17 | `riesgo_endeudamiento` NO es el indicador correcto para evaluar la calibración de intensidad de los arquetipos 2 (beneficio_sin_cash_flow) y 15 (riesgo_liquidez_pese_beneficio) — ambos usan `EfectoTesoreria`, que reduce `disponible` mediante `factor = max(0, 1 + dirección×intensidad_efectiva)` (`_disponible_proporcional_con_efecto`) ANTES de que el pipeline de déficit/deuda entre en juego. Medida su tasa de activación de `riesgo_endeudamiento` en el barrido estándar (27 sectores × 4 semillas), sale plana en 0,9% en las tres intensidades — a primera vista parece "insensibilidad" a la intensidad, igual que el patrón de 16 | **No es una falta de calibración: es que `riesgo_endeudamiento` mide algo que este mecanismo, por construcción, casi nunca llega a disparar.** Como el `factor` nunca es negativo y ninguno de los dos arquetipos toca masa_circulante (no hay déficit de NOF que sumar), `disponible_bruto_eur = disponible_proporcional_eur` es prácticamente siempre ≥0 — la vía "tesorería insuficiente → deuda a corto nueva → sube endeudamiento" casi nunca se activa. El indicador correcto es el propio mecanismo del arquetipo: la caída de `disponible/ventas`. Medido en el mismo barrido (27 sectores × 4 semillas, ambos arquetipos idénticos por compartir mecanismo): **disponible/ventas medio 23,17% (leve) → 19,94% (moderado) → 16,50% (fuerte)** — caída real, monótona y de magnitud razonable (~29% relativa entre leve y fuerte); el caso mínimo observado también desciende de forma consistente (3,55%→2,64%→1,61%) sin llegar nunca a 0 (0/216 casos en las tres intensidades). **Conclusión: 2 y 15 SÍ tienen un gradiente de intensidad real y sensible — no forman parte de ningún problema de calibración pendiente.** Si en el futuro se quiere seguir usando `riesgo_endeudamiento`/`contencion_al_limite` como criterio uniforme de "arquetipo con contención de plausibilidad" en algún informe o test, hay que recordar que para 2/15 (y en menor medida 8/16, por la misma razón estructural: su mecanismo tampoco alimenta el pipeline de déficit) esa señal no es representativa — el criterio correcto varía según qué variable toca cada mecanismo | — (verificación cuantificada, sin fix — el mecanismo ya funciona como debe) |
| 19 | Arquetipo 4 (deterioro_ciclo_caja) activa `riesgo_endeudamiento` mucho menos que el 1 (crecimiento_destruccion_caja) — 0,9%/0,9%/1,4% vs. 1,4%/6,5%/17,1% (leve/moderado/fuerte) — pese a tener un footprint de presión NOF proporcionalmente razonable (~80-85% del de 1, confirmado midiendo la magnitud real, no solo la tasa: `exceso_realizable` prácticamente idéntico al de 1 en las tres intensidades). ¿Es un problema de calibración del arquetipo 4? | **No — comportamiento explicado, no se toca.** Forzando el mismo `rango_crecimiento_pleno` del arquetipo 1 sobre el 4 (para aislar el efecto del crecimiento de ventas propio de 1) el hueco se cierra del todo en "leve" pero solo parcialmente en moderado/fuerte (34%/50% del hueco). Rastreado euro a euro, año por año (2024/2025), la causa real es un **efecto de umbral no lineal del colchón de tesorería, compuesto con la "espiral de deuda" ya documentada en el hallazgo #2**: la presión NOF de 1 es solo ~1,7-3× la de 4 (no desproporcionada), pero `disponible` actúa como un colchón que hay que agotar del todo antes de que aparezca deuda nueva — la presión moderada de 4 queda a menudo absorbida ENTERA por el colchón (deuda extra = 0 en varios casos observados), mientras que la mayor presión de 1 agota el colchón y genera deuda nueva en 2024 — una diferencia lineal pequeña en la presión se convierte en una diferencia grande en si se genera deuda o no. Esa deuda de 2024 (cuando existe) genera interés en 2025 que erosiona el PN — la misma espiral de deuda del arquetipo 9 — y en varios casos observados 2025 dispara `riesgo_endeudamiento` con presión NOF incremental de ESE año en CERO, puramente por el interés heredado de 2024. Como 4 casi nunca tomó deuda en 2024, casi nunca arrastra esa espiral a 2025, y ahí se concentra el hueco. **Son dos mecanismos ya existentes y ya documentados (colchón de tesorería + espiral de deuda) interactuando de forma no lineal pero explicable con un footprint proporcionalmente correcto — no un fallo nuevo del arquetipo 4.** Alguien auditando esto en el futuro no necesita repetir la investigación: la tasa distinta de 4 frente a 1 es la consecuencia esperable de esta interacción, no una señal de descalibración | — (verificación cuantificada — rastreo euro a euro por año, 4 semillas — sin fix, sin test nuevo: no hay comportamiento que fijar) |
| 20 | Recalibración de intensidad de los arquetipos 9 (y 14, variante exacta), 11 y 10 (ver #16 y #18): su señal de plausibilidad saturaba ya en "leve" (39,4%/41,7%/30,6% respectivamente, ninguno bajaba de ~7% ni con intensidad-efectiva casi nula para el 10 — suelo estructural, ver #18), muy por encima del resto de arquetipos bien calibrados (~1-7% en leve) | **Escalas de intensidad específicas por mecanismo**, sustituyendo la `INTENSIDAD_BASE` genérica (0,15/0,30/0,50) SOLO para estos 3 arquetipos (`INTENSIDAD_BASE_POR_ARQUETIPO` en `evolucion_arquetipo.py`) — calibradas empíricamente con el mismo barrido de siempre (27 sectores × 4 semillas), propuestas en 2 candidatas por arquetipo y aprobadas por el usuario tras revisión: apalancamiento (9/14) `0,02/0,08/0,25`; mejora_margen (11) `0,02/0,07/0,22`; mejora_ebitda (10) `0,015/0,06/0,22`. Resultado tras el cambio: 9 y 14 → 2,8%/14,4%/59,7% (idénticos entre sí, por compartir mecanismo exacto); 11 → 2,8%/15,7%/56,5%; 10 → 8,8%/16,2%/38,4% (por encima del objetivo de ~7% en leve por el suelo estructural del #18, es el mínimo alcanzable). **No hizo falta re-pinear ningún test**: la suite completa (333 tests) sigue en verde sin modificar ni un solo valor de referencia — los tests que dependen de la frecuencia de activación de estos 3 arquetipos ya estaban escritos de forma tolerante (`comprobados > 0`, "0 casos sin señalizar", nunca un recuento exacto atado a la escala de intensidad), así que siguen siendo válidos con la nueva escala sin tocarlos. **Nota para hallazgos #1/#2/#3/#6/#7/#10 (todos sobre 9/10/11/14, medidos con la escala antigua):** la mecánica de contención en sí (techo de endeudamiento, `_limitar_por_subtotal`, `_limitar_gastos_personal_por_baii`, espiral de deuda) no se ha tocado — solo la frecuencia con la que se activa — así que sus conclusiones cualitativas ("0 casos sin señalizar", "la contención siempre coincide con el límite por ruido") se mantienen (confirmado por los mismos tests, todavía en verde), pero **las cifras de frecuencia citadas en esos hallazgos (26/1.296, 130/1.296, 1.796/2.592, 316/648, 173/316 vs. 143/316, 53,3%/78,1%) corresponden a la escala de intensidad ANTERIOR (0,15/0,30/0,50) y ya no son representativas de la frecuencia actual** — no se han vuelto a medir con la escala nueva por no ser necesario para ninguna garantía de comportamiento, pero cualquiera que las cite debe saber que son históricas, no del estado actual. Efecto colateral esperado y confirmado en `#16`: el Combo F, que antes saturaba porque el arquetipo 9 solo ya estaba en 39,4%/67,6%/85,6%, ahora parte de una base mucho más baja | Suite completa (333 tests, sin modificar); `tests/test_combinacion_arquetipos.py` para el efecto sobre las combinaciones |
| 18 | **Hallazgo pendiente, fuera de alcance del encargo actual de recalibración de 9/10/11 — abordar como su propio encargo futuro (validación de plausibilidad de la sección 2.13 sobre el caso completo, no por arquetipo).** Al investigar por qué el arquetipo 10 (mejora_ebitda) tiene un suelo estructural del 6,9% en `riesgo_plausibilidad_pyg` incluso con intensidad prácticamente nula, se confirmó que `motor/empresa_base.py` (generación del ejercicio 2023 SIN ningún arquetipo activo) no aplica ningún techo/suelo de plausibilidad sobre `baii_pct` ni `margen_bruto_pct` como subtotales — solo trunca cada primitiva individual por separado (`_generar_partida`/`_normal_truncada`, máx. 1,5σ típico / 3,0σ atípico frente al huber de ESA primitiva, `PROB_ATIPICO=0,15`). `baii` se calcula combinando 4 primitivas ya truncadas independientemente (`valor_añadido − gastos_personal − amortizaciones + resultado_extraordinario`) sin ningún chequeo sobre el subtotal resultante; lo mismo para `margen_bruto = ingresos_explotacion − consumos_explotacion`. El único chequeo de subtotal que existe (`_limitar_por_subtotal`, `_limitar_gastos_personal_por_baii`) vive dentro de `evolucion_arquetipo.py` y solo se activa cuando un arquetipo empuja esa primitiva concreta — **un caso SIN ningún arquetipo activo (o con uno que no toque esas primitivas) puede tener `baii_pct`/`margen_bruto_pct` implausible sin que nada lo detecte hoy.** Evidencia cuantificada: barrido de referencia (27 sectores × 4 semillas) con intensidad ≈0 (arquetipo activo pero sin empuje real) — `riesgo_plausibilidad_pyg` se activa en el 6,9% de los ejercicios, igual en las tres intensidades — puramente por el ruido de la base, no por ningún arquetipo | No corregido — el chequeo pedido (techo/suelo de `baii_pct`/`margen_bruto_pct` sobre el caso completo, independientemente de si hay arquetipo activo) es un alcance mayor que toca `empresa_base.py`, no solo `evolucion_arquetipo.py`, y probablemente conviene abordarlo junto con el resto de la validación de plausibilidad de la sección 2.13 sobre el caso ya cerrado (los 3 ejercicios juntos), no arquetipo a arquetipo | — (hallazgo documentado, sin test — pendiente de encargo futuro) |
| 21 | Para el EFE (sección 2.2/2.5) hacía falta decidir qué `ventas_objetivo` usar por defecto para cada segmento del catálogo — la convención de todos los tests hasta ahora (8.000.000 € para "grandes_medianas", el único segmento realmente usado en `tests/test_arquetipos_*.py`) resultó, al clasificar legalmente el caso (Art. 257 LSC, ver `motor/clasificacion_legal.py`), invertida el 65,7% de las veces (clasifica "abreviado" siendo "grandes_medianas") — y "pequeñas" a la misma cifra, "normal" el 68,5% de las veces. ¿Invalida esto alguna conclusión cuantitativa ya cerrada del proyecto, medida siempre a 8M€? | **No, verificado, no asumido.** Se confirmó por barrido (27 sectores x 4 semillas, arquetipos 1 y 9, las 3 intensidades) que las tasas de activación de `riesgo_endeudamiento`/`riesgo_plausibilidad_pyg` son EXACTAMENTE idénticas a 8M€, 15M€ y 40M€ de ventas — el motor es escala-invariante por diseño (todo se ancla a ratios/porcentajes del catálogo, no a importes absolutos), así que ninguna conclusión de calibración de intensidad (9/10/11/17/18, etc.) depende del nivel de ventas usado. **Única excepción confirmada, y esperada por diseño:** los suelos defensivos en euros ABSOLUTOS de los arquetipos 20/21 (`SUELO_IMPORTE_VINCULADAS_EUR=30.000€`, `SUELO_NOCIONAL_COBERTURA_EUR=50.000€`, ver hallazgo #14) sí dependen de la escala — medido: el suelo de 20 se activa en el 25,0% de los casos a 2M€ de ventas, 8,3% a 8M€, 3,7% a 15M€. No invalida el hallazgo #14 (esos tests siguen fijos a 8M€, sin tocar), pero confirma que un suelo defensivo en euros es, por construcción, sensible a la escala. **Convención adoptada para el EFE y cualquier trabajo futuro que dependa del segmento:** `ventas_objetivo` representativa de cada segmento en vez del valor plano de 8M€ — 5.000.000 € para "pequeñas" (dentro del <=10M€ de ACCID), 15.000.000 € para "grandes_medianas" (por encima del >10M€ de ACCID) — documentado también en `CLAUDE.md` | Barrido de escala-invarianza (script ad hoc, no un test permanente) |
| 22 | Comprobación obligatoria pedida antes de generar EFE: ¿el segmento del catálogo ("pequeñas"/"grandes_medianas", usado solo para elegir la fila Huber de partida) coincide con la clasificación legal real (Art. 257 LSC, 2-de-3 sobre activo/cifra de negocio/plantilla estimada) del caso ya generado? | **No, y NO se fuerza a que coincida — son dos clasificaciones independientes por diseño, ambas expuestas explícitamente en cada caso (ver `motor/clasificacion_legal.py` y "Clasificación legal" en `CLAUDE.md`).** Con `ventas_objetivo` ya representativa del segmento (ver #21): "grandes_medianas" a 15M€ clasifica siempre "normal" (0/108 incoherente) — se resuelve solo. "Pequeñas" a 5M€ sigue clasificando "normal" (no abreviado) en el 27,8% de los casos (30/108) — no repartido uniformemente: 10 de 27 sectores tienen algún caso así, y 6 fallan en las 4 semillas sin excepción (Investigación y desarrollo (72), Contabilidad/auditoría/asesoría fiscal (69.2), Programación y consultoría informática (62), Educación (85), Actividades hospitalarias (86.1), Construcción aeronáutica y espacial (30.3)) — en los 6, fallan simultáneamente activo Y plantilla estimada pese a facturación modesta (ej. Educación: activo=5,7M€, plantilla estimada=80, con solo 5M€ de ventas). **Es un reflejo económico real, no un defecto del motor ni de la estimación de plantilla**: son sectores intensivos en personal y/o con baja rotación de activo en el propio catálogo ACCID para el segmento "pequeñas" — una empresa de consultoría o de I+D con facturación modesta puede legítimamente necesitar más de 50 empleados, y el Art. 257 LSC no exime a una empresa de "modelo normal" solo por facturar poco si falla los otros dos criterios. Consecuencia práctica para el EFE: la decisión de generar/omitir el EFE de cada caso debe basarse en la clasificación legal real (`clasificar_par_ejercicios`), NUNCA en el segmento solicitado | `motor/clasificacion_legal.py` — sin test de regresión todavía (pendiente del resto de la implementación del EFE) |
| 23 | ECPN — Documento A (Estado de Ingresos y Gastos Reconocidos), ¿se implementa junto al Documento B? | **Aparcado, decisión tomada por el usuario, no implementado.** Exige movimientos de las cuentas de grupo 8/9 (valoración de instrumentos financieros, coberturas de flujos de efectivo, subvenciones, actuariales, activos mantenidos para la venta, diferencias de conversión) que este motor no cuantifica hoy — ni el arquetipo 21 (coberturas) ni el 19 (activo mantenido para la venta) producen un ajuste de PN cuantificado, solo notas de memoria sin cifra de balance asociada. Además, el Documento A solo es legalmente obligatorio para GRANDES empresas — ni siquiera para el resto de modelo normal — así que aunque se construyera, la mayoría de los casos generados no lo necesitarían de todos modos. Revisitar si en el futuro se decide cuantificar esos ajustes de PN (p. ej. dar cifra a la cobertura del 21 más allá de la nota narrativa) | — (aparcado, sin test — no hay comportamiento que fijar) |
| 24 | Desagregación de Patrimonio Neto (Capital / Reservas y resultados de ejercicios anteriores / Resultado del ejercicio), aplicada a TODOS los balances que genera el motor (no solo el ECPN) — HIPÓTESIS DE DISEÑO, el catálogo ACCID nunca desglosó PN más allá del agregado. ¿Qué criterio de reparto usar, y sale "Reservas" con magnitudes disparatadas en los casos más agresivos? | Capital social = fracción de PN(2023) (centro 40%, rango 30-50%, ruido mixto típico/atípico ya existente sobre esos números — no hay ratio del catálogo para esto, así que se usa `_generar_partida` con un centro/dispersión definidos aquí, no un dato sectorial), redondeado a una cifra vistosa (2 cifras significativas, `_redondear_cifra_vistosa`), FIJO desde el año base (2023) y constante en 2024/2025 — un sorteo único por caso, añadido al FINAL de la secuencia de rng ya existente en `generar_empresa_base` (no desplaza ningún sorteo anterior, 0 valores de referencia modificados). Reservas se derivan por resta: `PN(t) − capital_social − resultado_ejercicio(t)` — no es una fuente de ruido propia. **Comprobación de "Reservas no absurdamente negativa"** (barrido: 21 arquetipos en solitario + 6 combinaciones, 27 sectores, 4 semillas, intensidad fuerte, `ventas_objetivo=15M€`): 36/3.456 ejercicios (1,04%) con reservas negativas en arquetipos solos, 16/1.296 (1,23%) en combinaciones — ambos casos minoritarios, y el peor caso en cada grupo coincide con un ejercicio de PN YA negativo o muy erosionado por mecanismos ya documentados (la "espiral de deuda" del hallazgo #2, aquí también observada para el arquetipo 18 en un sector de muestra ACCID poco fiable — sector 30.3, ver aviso de fiabilidad muestral de la sección 2.22) — no un síntoma de que el criterio de reparto de capital esté mal calibrado, sino la consecuencia esperable de un PN ya deteriorado por causas ya conocidas. Aceptado tal cual, mismo criterio que otros extremos ya aceptados en el proyecto (PN negativo del arquetipo 9) | `tests/test_desagregacion_pn_y_activo.py::test_reservas_no_absurdamente_negativas_en_barrido_amplio` (umbral de referencia <5%) |
| 25 | Desagregación de `activo_no_corriente` (material / intangible / inversiones inmobiliarias / otros activos financieros), necesaria para el desglose de inversión del EFE — HIPÓTESIS DE DISEÑO, el catálogo ACCID nunca desglosó activo_no_corriente. ¿Qué perfil usar por sector, y se mantiene coherente con la categoría económica del sector bajo ruido? | Perfil por CATEGORÍA de sector (las 8 categorías de la sección 2.22: industria, servicios_industriales, servicios_profesionales_tic, transporte_logistica, comercio_hosteleria, construccion, administracion_educacion_sanidad, inmobiliario), no un único reparto para los 27 sectores — cada perfil suma exactamente 1,0 (`PERFIL_ACTIVO_NO_CORRIENTE_POR_CATEGORIA`). Ruido mixto típico/atípico ya existente sobre cada componente (dispersión sintética del 20% relativo al propio valor, sin MAD de catálogo detrás — documentado como tal), renormalizado a 100% tras el ruido. Sorteo único por caso (no por año), aplicado cada año al total de `activo_no_corriente` de ESE año (el capital social y el perfil son ambos rasgos ESTRUCTURALES fijos del caso, análogos a `rotacion_activo`). El salto del arquetipo 18 (adquisición) se mantiene aparte del perfil (campo `incremento_activo_adquisicion_eur`), no se reparte por perfil. **Verificado (27 sectores, hasta 10 semillas)**: los 4 componentes siempre suman exactamente 1,0 (0 fallos), ningún componente sale negativo, y el componente DOMINANTE del perfil nunca cambia respecto al esperado de su categoría bajo ruido (0/270 casos con dominancia volteada — p. ej. ninguna fábrica sale con más intangible que material, ningún sector profesional/TIC con más material que intangible) | `tests/test_desagregacion_pn_y_activo.py` (4 tests: suma=100%, sin negativos, dominancia estable, reconciliación con evolución completa) |
| 26 | Mapeo completo de mecanismos del motor a líneas del EFE (modelo normal PGC, verificado externamente línea por línea) y del ECPN Documento B, revisado por el usuario antes de implementar — **corrección importante sobre un punto que se había dado por bueno en la ronda de revisión anterior**: la amortización (A.2.a) | El punto (d) de la ronda de mapeo anterior ("amortización se cancela exactamente dentro de A1+A2") estaba articulado de forma imprecisa y, tal como se había propuesto entonces (añadirla de vuelta en A.2.a), habría roto la reconciliación en exactamente el importe de la amortización de cada año — verificado con un caso mínimo antes de escribir el código definitivo. La causa: este motor NUNCA reduce `activo_no_corriente` por amortización acumulada (no hay contra-cuenta de amortización acumulada modelada) — así que, en términos de la identidad del balance de ESTE motor, la amortización se comporta como un gasto en efectivo cualquiera: la reducción de PN que causa la absorbe automáticamente el parche de cuadre (`otras_deudas_corto`, ya mapeado a A.3.e) — nunca una reducción de activo. Añadirla de vuelta en A.2.a duplicaría ese efecto. **Decisión final: `a2a_amortizacion` se deja SIEMPRE en 0,0** — una desviación deliberada y documentada del modelo de texto, no un olvido; los demás ajustes de A.2 (ingresos/gastos financieros) sí se incluyen tal cual porque se cancelan exactamente dentro del propio EFE (A.2.g/h se deshacen en A.4.c/a), sin depender de si el balance tiene o no una partida propia detrás — verificado con la misma lógica. El resto del mapeo (apalancamiento como flujo bruto C.10+C.11 que se cancela, `otras_deudas_corto`→A.3.e, `otras_deudas_largo`→A.3.f, perfil de `activo_no_corriente`→B.6/7 por componente, salto de adquisición→B.6.a aparte, deuda financiera total→C.10 en variación neta sin desglose emisión/devolución bruto por no tener dato para distinguirlo) se implementó tal como se propuso y aprobó. **Reconciliación exhaustiva** (21 arquetipos en solitario + 6 combinaciones + 1 caso de intensidad ≈0 como aproximación de "sin arquetipo", 27 sectores, 4 semillas, 2024 y 2025): **4.968 EFE y 4.968 ECPN generados, 0 descuadres en ambos, 100% de reconciliación exacta** (tolerancia 0,01€) | `tests/test_efe.py`, `tests/test_ecpn.py` (barrido representativo permanente: 16 arquetipos × 3 sectores × 3 semillas + 6 combos × 3 sectores × 3 semillas, 2024 y 2025, más tests específicos de líneas a 0 y del tratamiento de amortización) |
| 27 | Arreglo de raíz de la amortización (causa del hallazgo #26: `a2a_amortizacion` siempre a 0€ porque no había ningún activo real detrás del gasto) — verificación normativa PGC antes de generar nada | **Corrección importante respecto a la premisa del encargo**: el fondo de comercio SÍ se amortiza sistemáticamente en España desde el 1-1-2016 (Ley 22/2015 + RD 602/2016, PGC norma 6ª, 10 años presuntos) — al revés de lo planteado ("no se amortiza desde 2016"). Es una particularidad española: IFRS para cotizadas sigue sin amortizarlo, el PGC individual sí, desde esa fecha. Verificado con 3 fuentes independientes citando la norma literalmente. Consecuencia: dentro de `intangible`, prácticamente todo es amortizable bajo PGC. Terrenos (en material E inversiones inmobiliarias) y otros activos financieros: nunca amortizables, confirmado sin sorpresas. Tabla de coeficientes fiscales verificada contra Agencia Tributaria (tabla vigente desde 2015, Ley 27/2014 IS) — "vida fiscal" = 100/coeficiente MÁXIMO (la más corta), no el "período máximo" de la tabla oficial (que corresponde al coeficiente mínimo) | `motor/amortizacion.py` (docstring del módulo, con las 3 fuentes citadas) |
| 28 | Diseño de generación de activos amortizables: cohorte por sub-tipo (7 sub-tipos de material + 2 de intangible + construcción de inversiones inmobiliarias = 9 buckets amortizables por caso), corregido a 3 SUB-LOTES independientes por bucket (no 1) tras detectar que un único sorteo binario de "ya totalmente amortizado" por bucket implicaba comprar toda una categoría el mismo día — poco realista | Perfiles de sub-tipo por las 8 categorías de sector (hipótesis de diseño razonada, sin acceso en este entorno a la Central de Balances del Banco de España para contraste externo — declarado explícitamente como tal, no presentado con falsa autoridad estadística). **Verificación de suavidad pedida explícitamente** (80 semillas, sector 24.1): distribución de amortización derivada sin clustering discreto — el hueco más grande entre valores consecutivos (173.729€) está aislado en el extremo superior de la cola (semilla más atípica de 80), el resto de huecos ronda 18.000-46.000€ sobre un rango total de ~900.000€ — consistente con variación continua, no con "escalones" de un sorteo binario grueso. Fórmula del sorteo de "ya totalmente amortizado" ya verificada por el usuario antes del encargo (vida 3→1/3, vida 5→~1/7,35, vida 20→1/100 exactos) — reproducida sin cambios | Verificación cuantificada ad hoc (script, no test permanente — la suavidad no es una propiedad que fijar con una aserción exacta, es una comprobación de forma de la distribución) |
| 29 | Reconexión con `amortizaciones_pct` ya calibrado del catálogo — validación cuantificada, mismo método de siempre (27 sectores × 8 semillas, comparando la media derivada contra el Huber del catálogo) | **RESUELTO — ver #33.** La mayoría de sectores (≈20 de 27) caen en una banda razonable (0,7x-2,5x el huber — variabilidad genuina esperada, ahora real en vez de sorteada). Dos sectores mostraban una desviación SISTEMÁTICA severa: Contabilidad/auditoría (69.2, 10,1x) y Consultoría de gestión (70.2, 7,8x) — ambos en la categoría combinada original "servicios_profesionales_tic". Investigado: la causa NO era el perfil de sub-tipos (se probó reducir `aplicaciones_informaticas` del 60% al 25% dentro de intangible — apenas cambió el ratio) — la causa raíz era que estos 2 sectores tienen un `rotacion_activo` inusualmente BAJO en el catálogo (0,28-0,30, activo_no_corriente = 2,5-2,8x los ingresos), combinado con el perfil de NIVEL SUPERIOR (55% de `activo_no_corriente` a intangible) que trataba "servicios profesionales" y "TIC" como una sola categoría — separados en #33, queda corregido a un orden de magnitud razonable | Ver #33 |
| 30 | Comprobación obligatoria: identidad `Balance.PN.resultado_ejercicio == PyG.resultado_ejercicio`, tras el cambio en cómo se calcula la amortización dentro de la propia PyG | **0 fallos en 7.128 ejercicios comprobados** (21 arquetipos en solitario + 6 combinaciones, 27 sectores, 4 semillas, 3 años). La identidad se mantiene EXACTA por construcción, no por casualidad: `EjercicioEmpresa.reservas_eur` se deriva literalmente como `PN − capital_social − pyg_eur["resultado_ejercicio"]`, usando la MISMA referencia al valor de PyG recién calculado, nunca una copia cacheada — no había ningún punto del código donde balance y PyG pudieran desincronizarse (a diferencia del sesgo de RNG compartido del hallazgo histórico, aquí no se encontró ningún problema real, la comprobación salió limpia desde el principio) | `tests/test_amortizacion.py::test_identidad_balance_pyg_y_reconciliacion_efe_ecpn_*` |
| 31 | Impacto en EFE/ECPN tras conectar la amortización real: ¿la reconciliación sigue cuadrando al 100%? | **Sí — 0 descuadres en 7.128 EFE y 7.128 ECPN** (mismo barrido que el hallazgo #30). `a2a_amortizacion` del EFE sigue en 0,0 (arreglo aplazado al encargo del EFE, tal como se confirmó) — el cambio de este encargo solo afecta a CUÁNTO vale `resultado_ejercicio`/PN cada año, no a la mecánica de reconciliación en sí, que sigue absorbiendo correctamente cualquier residuo vía `otras_deudas_corto` (A.3.e) como ya estaba diseñado | `tests/test_amortizacion.py`, `tests/test_efe.py`, `tests/test_ecpn.py` |
| 32 | Impacto en pruebas ya cerradas: al eliminarse el sorteo independiente de `amortizaciones_pct` (una posición menos en el bucle de primitivas de PyG), se desplaza la secuencia de `rng_pyg` para `resultado_extraordinario`/`ingresos_financieros`/`impuesto_beneficios` — ¿qué se rompe? | Mucho menos de lo esperado: **el perfil de nivel superior de `activo_no_corriente` y la colección de activos se generan con un RNG independiente y propio** (no el `rng` compartido de balance/PyG), así que `balance_pct`, `rotacion_activo`, `activo_no_corriente`, existencias/realizable/disponible del año 2023 NO cambiaron en absoluto. Solo cambiaron: el PyG de 2023 en adelante (amortización ya no sorteada + su cascada) y, por un efecto en CASCADA legítimo (PN distinto → endeudamiento distinto → la contención de plausibilidad amortigua una cantidad distinta), las masas de circulante de 2024/2025 en los casos donde la contención se activa — exactamente lo que le pasó al test de valores exactos del arquetipo 1 (sector 24.1, semilla 5, fuerte), re-pinneado con los nuevos valores (2024: existencias 2.861.635€ endeudamiento 0,677; 2025: existencias 3.422.807€ endeudamiento 0,747 — 2023 sin cambios). Suite completa: 548 tests, 0 fallos tras los 7 ajustes esperados (6 asserts de `modo in (...)` que ahora aceptan también "derivado", 1 re-pinneo) | Suite completa (548 passed) |
| 33 | Resolución del hallazgo #29: separar la categoría combinada "servicios_profesionales_tic" en DOS perfiles de nivel superior distintos — "servicios_profesionales" (69.2 Contabilidad/auditoría, 70.2 Consultoría) y "servicios_tic" (62 Programación) — bajo la hipótesis de que tratarlas como una sola categoría (55% de `activo_no_corriente` a intangible para las tres) encajaba con 62 pero no con 69.2/70.2 | **Confirmado — corrige a un orden de magnitud razonable, aceptado como cerrado.** Nuevo perfil "servicios_profesionales": `material=15%, intangible=15%, inversiones_inmobiliarias=5%, otros_financieros=65%`. **Importante — naturaleza de esta asignación**: `otros_financieros` como componente dominante NO es una afirmación verificada sobre la estructura real de balance de estas empresas; es la mejor ubicación disponible dentro de nuestro modelo para absorber la desviación detectada (rotacion_activo 0,28-0,30, ver #29) sin inventar una masa patrimonial que el motor no modela. La idea de que pueda corresponder a participaciones/inversiones financieras propias de estructuras de holding (habituales en redes de auditoría/consultoría con despachos asociados) es una hipótesis plausible, no contrastada con fuente externa — se documenta como tal, y queda abierta a revisión si en el futuro aparece un dato más fiable sobre la composición real del activo de estos 2 sectores. Lo que sí está verificado cuantitativamente es que la reasignación corrige el orden de magnitud de la desviación (ver resultado más abajo), que es el objetivo que motivó el cambio. "servicios_tic" mantiene un perfil intangible-alto (`material=20%, intangible=50%, otros_financieros=27%`), sin cambios sustanciales respecto al original, porque 62 nunca mostró el problema (ratio ya en 0,97x-1,22x antes de separar). **Resultado de la validación tras la separación**: 69.2 pasa de 10,06x a **3,88x**; 70.2 de 7,79x a **3,25x**; 62 se mantiene en 1,03x (sin apenas cambio). 3,2-3,9x ya no es "10x" — queda en el mismo orden de magnitud que otros sectores ya aceptados con desviación moderada (construcción 41.2 en 4,08x, hoteles 55.1 en 2,48x) — el residuo restante se acepta como reflejo de una característica real y ya documentada del dato de rotación de activo de ACCID para estos 2 sectores concretos, no como un fallo del perfil. Suite completa sigue en verde (548 tests) tras el cambio — la separación no rompió ningún test existente (el test de "componente dominante nunca se voltea" recalcula el esperado desde el propio diccionario de perfiles, así que se adapta automáticamente a las dos categorías nuevas) | Suite completa (548 passed); validación ad hoc re-ejecutada (tabla antes/después en la conversación con el usuario) |
| 34 | Encargo de coberturas/subvenciones: alcance normativo aprobado — solo 2 operaciones de grupo 8/9 (coberturas de flujos de efectivo, arquetipo 21; subvenciones de capital, transversal) + el efecto impositivo (subgrupo 83), no las 6 familias completas del PGC. Confirmar que las descartadas siguen fuera correctamente | Sin cambios respecto a lo ya documentado en `motor/ecpn.py` antes de este encargo (coberturas de inversión neta en el extranjero, diferencias de conversión, FVOCI, pérdidas actuariales — sin mecanismo, se agregan en `b_resto_fuera_de_alcance`/`c_resto_fuera_de_alcance`, siempre 0.0, documentado explícitamente en el Documento A). **Activos mantenidos para la venta (arquetipo 19) reconfirmado que NO dispara grupo 8/9** — su nota de memoria (motor.memoria) describe una reclasificación de balance/deterioro, no un ajuste de valor a través de patrimonio neto (NRV 7.ª: los activos no corrientes mantenidos para la venta se valoran al menor de su valor contable y valor razonable menos costes de venta, con la corrección yendo a PyG como deterioro, no a una reserva de PN) — la asunción inicial de la sección de diseño (previa a esta implementación) de que sí lo haría era errónea, ya corregida antes de escribir código | `motor/ecpn.py` (docstring del módulo) |
| 35 | Diseño del cuadre exacto (Activo=Pasivo+PN) para las 2 operaciones nuevas — no es evidente por simple "razonamiento contable correcto": ¿basta con las cuentas oficiales del PGC (810/910↔176/255/559, 940↔57/470, 8301↔479/4740) tal cual, o hace falta una decisión de implementación adicional? | **Hace falta una decisión adicional, y no es la misma para las 2 operaciones — verificado por álgebra ANTES de escribir código, no ajustado a posteriori.** Los importes que se reciclan a la PyG (transferencia por vencimiento + ineficacia de la cobertura; imputación anual de la subvención) deben inyectarse en la cascada de PyG por su importe BRUTO completo (no neto de impuesto) y DESACOPLADOS del `impuesto_beneficios_pct` genérico ya sorteado (que no puede gravar/desgravar exactamente esta partida concreta sin romper el cuadre por una fracción de euro). Derivación verificada para ambas operaciones: el saldo bruto de cada reserva de PN (1340 para cobertura, 130 para subvención) evoluciona con su propia fórmula; la cobertura coloca en balance el valor razonable BRUTO completo del derivado (incluida la ineficacia, que nunca entra en la reserva 1340) mientras que la subvención coloca un cobro de caja FIJO (no sigue el saldo de la reserva 130, que sí se agota con el tiempo) — esta asimetría es real (deriva de la naturaleza distinta de ambas operaciones, no de una elección arbitraria) y exige que el importe reciclado a PyG sea el bruto íntegro en los dos casos para que el balance siga cuadrando exactamente sin depender del plug de "otras deudas a corto" | `tests/test_amortizacion.py` (identidad Balance↔PN extendida), `tests/test_efe.py`, `tests/test_ecpn.py`, `tests/test_coberturas_subvenciones.py` — 0 descuadres |
| 36 | Verificación de plausibilidad OBLIGATORIA de la magnitud de la cobertura (pedida explícitamente, no asumir que la fórmula financiera "correcta" da magnitudes razonables) | **Se necesitaba, no era una comprobación de trámite.** Barrido de 27 sectores × 4 semillas × 3 intensidades del arquetipo 21 en solitario: sin acotar `Δr`, el peor caso (sector 56.1, Restaurantes y puestos de comidas — ya identificado en otros hallazgos del proyecto como sector pequeño y volátil) llegaba a un ajuste de PN del **13,7% del balance total**, por un `Δtipo_interes` año a año de hasta 20 puntos porcentuales — real dentro de este motor (`tipo_interes` es un coste de deuda sintético por sector que mezcla tipo de referencia + prima de riesgo/spread de crédito, con volatilidad propia mucho mayor que la de un Euríbor real), pero desproporcionado para lo que un IRS real cubre (solo el tipo de referencia). **Corrección**: `TECHO_DELTA_R_ANUAL = 0.03` (300 puntos básicos, generoso pero acotado) en `motor/coberturas_subvenciones.evolucionar_cobertura` — tras acotar, el peor caso del mismo barrido baja a **2,1%** del balance | `tests/test_coberturas_subvenciones.py::test_plausibilidad_cuantitativa_del_ajuste_de_cobertura` |
| 37 | Obligatoriedad del Documento A (EIGR) — el encargo pide verificar el criterio legal exacto de "gran empresa" contra el PGC/normativa, no asumirlo igual al de modelo abreviado/normal (Art. 257 LSC) | **Corrección de una afirmación previa del propio código** (el docstring de `motor/ecpn.py`, escrito en el encargo anterior del EFE/ECPN, decía que el Documento A "solo es legalmente obligatorio para grandes empresas — ni siquiera para el resto de modelo normal", una distinción no verificada en su momento). Verificado ahora explícitamente (búsqueda externa): el PGC (RD 1514/2007, Normas de Elaboración de las Cuentas Anuales) permite un "ECPN ABREVIADO" (un único documento equivalente al Documento B, sin Documento A) a las empresas que pueden formular balance y memoria abreviados — el MISMO test de 2-de-3 del Art. 257.1 LSC ya implementado en `motor.clasificacion_legal`. La referencia histórica encontrada (2.850.000 €/5.700.000 €/50 trabajadores) es el umbral ORIGINAL de 2007 de ese mismo Art. 257 LSC antes de sus actualizaciones posteriores (a los 4.000.000 €/8.000.000 €/50 vigentes que ya usa este motor), no un umbral distinto y más estricto de "gran empresa" — no existe, en el texto del PGC, una tercera categoría separada de "modelo normal" a estos efectos. Documento A y Documento B comparten ahora el MISMO flag `obligatorio` — no se ha añadido ningún clasificador legal nuevo | `motor/ecpn.py` (docstring corregido), `tests/test_coberturas_subvenciones.py::test_eigr_obligatorio_comparte_flag_con_ecpn` |
| 38 | Hipótesis de diseño: propensión de fondo de la subvención de capital por categoría de sector (sin arquetipo 17 activo) — `PROPENSION_SUBVENCION_BASELINE_POR_CATEGORIA` | Documentada explícitamente como hipótesis de diseño en `motor/coberturas_subvenciones.py`, sin dato ACCID que la ancle (el encargo ya lo anticipaba: "sin dato ACCID que lo ancle"): industria (incluye agroalimentario dentro de esa categoría) y sectores tecnológicos/con tradición de ayudas públicas a la inversión más altos (0,15-0,20), servicios puros (profesionales, comercio/hostelería, inmobiliario) casi nulos (0,02) — mismo patrón cualitativo ya usado para los perfiles de `activo_no_corriente` por categoría (razonamiento de negocio explícito, no dato verificado) | Verificado en barrido que se activa con la frecuencia esperada (14 de 162 casos-año en un barrido de 27 sectores × 6 semillas con un arquetipo sin relación, `exceso_stock`) y que sigue cuadrando exactamente cuando se activa |
| 39 | Interacción detectada entre la subvención "de fondo" y la contención de plausibilidad de `margen_bruto` del arquetipo "mejora_margen" (`_limitar_por_subtotal`, techo ya existente) | La contención actúa ANTES de inyectar el ajuste de grupo89 (que ocurre más tarde en la cascada, tras sortear el tipo de interés) — si la probabilidad de fondo de subvención coincide con un caso de "mejora_margen" ya pegado a su techo sectorial, el margen puede acabar unos pocos puntos-base por encima (detectado: sector 62, semilla 1, 2025, margen_bruto +0,017 puntos sobre el techo). **No es un fallo de la contención ni de la subvención — es un efecto real** (la subvención SÍ sube el margen, es ingreso de explotación) que la contención (diseñada antes de que existiera este mecanismo) no anticipa. Aceptado como limitación documentada, no corregido con una segunda pasada de contención (coste de ingeniería desproporcionado para una desviación de centésimas de punto en una coincidencia de 1/24 casos del barrido) — el test correspondiente excluye explícitamente los ejercicios con `subvencion_transferencia_bruto_eur > 0` de la comprobación estricta, con el motivo documentado en el propio test | `tests/test_arquetipos_generalizados.py::test_margen_bruto_nunca_supera_el_techo_del_sector` |
| 40 | Verificación exhaustiva final del encargo: identidad Balance↔PN, EFE, ECPN (Documento B) y Documento A (EIGR) con los dos mecanismos nuevos activos, sobre el barrido estándar del proyecto | **0 descuadres.** Identidad Balance↔PN (extendida con las 2 líneas nuevas): 0 fallos en los 21 arquetipos (ahora incluyendo "coberturas") + 6 combinaciones, 3 sectores × 3 semillas — mismo barrido que ya usaba `test_amortizacion.py`. EFE: 0 descuadres (mismo barrido, más un barrido dedicado de 1.296 ejercicios de "coberturas"/"capex_elevado" en solitario sobre los 27 sectores × 4 semillas × 3 intensidades). ECPN Documento B: 0 descuadres, y su fila "Total de ingresos y gastos reconocidos" coincide EXACTAMENTE (verificado numéricamente, no solo algebraicamente) con la fila D del Documento A en todos los casos comprobados. Verificación cruzada de las 3 bandas de la subvención (EFE C.9, línea de PN neta, imputación en PyG) — las 3 cuadran ENTRE SÍ, no solo cada una por separado. Suite completa: **561 tests, 0 fallos** | Suite completa (561 passed) |
| 41 | Diagnóstico de causa raíz pedido tras detectar (validación de plausibilidad del encargo de coberturas) que `tipo_interes` podía saltar hasta 35pp entre años en sectores "normales" (no solo el ya señalado 56.1) — ¿es un error de extracción de la Fase 1, o el propio ratio `ratios.coste_deuda` mide algo distinto de lo asumido? | **El ratio mide lo que se asumía (`Gastos financieros / Préstamos`, verificado contra el texto literal del informe ACCID), y el dato extraído es 100% fiel a la fuente** (verificado a mano, renderizando páginas del PDF con PyMuPDF — `pdftotext` desalinea esta tabla, no usar) — 5/5 puntos comprobados (sectores 24.1, 30.3, 56.1, 62, 72, ediciones 2022 y 2024) coinciden exactamente con el catálogo. **La causa real**: "Gastos financieros" (PGC) incluye más que intereses (deterioros de activos financieros, diferencias de cambio, pérdidas en enajenación de instrumentos financieros) y "Préstamos" puede ser una fracción minúscula del pasivo en sectores financiados mayoritariamente con pasivo no financiero — confirmado con el desglose de balance/PyG de la misma página del PDF para 24.1 y 30.3: la deuda financiera formal se desplomó mientras los gastos financieros se mantuvieron altos, produciendo valores de 74%-110%. No es un fallo de extracción — es que el ratio, tal como ACCID lo define y publica, no es una fuente fiable de "tipo de interés medio" para este uso | Los 3 PDFs originales de ACCID (2018/2021/2024) quedaron comiteados en `docs/` para verificación futura |
| 42 | Diseño de la fuente de mercado que sustituye `ratios.coste_deuda` para `tipo_interes` — niveles de referencia, estructura de prima de riesgo, mecanismo de ruido | Diseño en 3 capas, propuesto y aprobado antes de implementar: (1) **Euríbor 12M por año** (`REFERENCIA_EURIBOR_12M_POR_AÑO`), verificado externamente (no de memoria) — 2023=3,87%, 2024=3,27%, 2025=2,22%, reflejando la forma real (pico post-subidas BCE en 2023, recortes desde junio 2024, estabilización más baja en 2025); (2) **prima de riesgo por categoría de sector** (`PRIMA_RIESGO_POR_CATEGORIA`, las mismas 9 categorías de perfil ya usadas para `activo_no_corriente`/propensión de subvención) — hipótesis de diseño razonada con conocimiento financiero general (más colateral/demanda estable → prima menor: administración/sanidad 0,60pp, inmobiliario 0,80pp; menos activo pignorable o mayor riesgo cíclico → prima mayor: TIC 1,75pp por intangibilidad, construcción 1,60pp por riesgo histórico en España), más un recargo plano para "pequeñas" (`RECARGO_TIPO_INTERES_PEQUEÑAS_PP=0,75pp`, no diferenciado por sector, sin base razonada sólida para variarlo); (3) **ruido mixto típico/atípico** entre empresas del mismo sector/segmento/año, mismo mecanismo de siempre, dispersión de diseño `DISPERSION_TIPO_INTERES_PP=0,50pp` (sin MAD de catálogo del que derivarla). `SUELO_TIPO_INTERES`/`TECHO_TIPO_INTERES` bajados de 1%/40% a 1,5%/12% (el 40% se alcanzaba rutinariamente con la fuente contaminada, ahora es un techo defensivo genuino) | Fuentes: búsqueda cruzada Banco de España/euribor.com.es/hipotecasyeuribor.com (Euríbor); ver #44 para el anclaje externo de la prima |
| 43 | Recalibración de la prima de riesgo ANTES de implementar: la primera propuesta de niveles (basada solo en razonamiento cualitativo) ¿queda bien anclada en nivel absoluto, o solo en orden relativo? | **No quedaba bien anclada — se corrigió antes de escribir código.** La media de las 9 primas propuestas inicialmente (1,70pp) implicaba un tipo medio para "grandes_medianas" sistemáticamente ~0,3-0,6pp por encima del dato real de Banco de España (tabla 19.6, tramo >1M€: prima media implícita real ~1,20pp). Se desplazó el conjunto completo de primas hacia abajo en 0,49pp (conservando el orden relativo entre categorías, que sigue siendo hipótesis cualitativa no verificada punto por punto) para que la media coincidiera con el ancla externa — mismo criterio que "ajustar el parámetro de generación, no forzar el resultado" ya aplicado en la reconexión de `amortizaciones_pct` (#29/#33) | Ver #44 para el resultado tras la recalibración |
| 44 | Validación externa independiente (Banco de España) y cierre del círculo: ¿el tipo de interés generado se parece a tipos reales por tramo de tamaño, y el problema original (74%-110%) deja de reproducirse? | **Ambas cosas, verificadas cuantitativamente, no asumidas.** (a) Banco de España, Boletín Estadístico, tabla 19.6 ("Tipos de interés TAE de nuevas operaciones... Sociedades no financieras" por tramo de importe) — comparado el tipo medio generado (27 sectores × 5 semillas × 3 años, por segmento) contra el tramo >1M€ ("grandes_medianas") y <250mil€ ("pequeñas"): desviación máxima **0,4pp** en los 6 puntos comprobados (2023/2024/2025 × 2 segmentos) — p. ej. grandes_medianas 2024: generado 4,40% vs. BdE 4,35%. (b) Cierre del círculo: recalculado `gastos_financieros / deuda_financiera` (el cálculo exacto de ACCID) sobre **8.262 ejercicios** generados (17 arquetipos cuantitativos × 27 sectores × 3 semillas × 2 segmentos, incluidos los arquetipos que más mueven deuda: apalancamiento, capex_elevado, adquisición) — **máximo 6,81%**, muy por debajo incluso del techo de estrés pedido (12%-15%), y nada parecido a los 74%-110% del hallazgo original | Barrido ad hoc (script, no test permanente) — el techo/suelo (1,5%-12%) sí queda cubierto por `tests/test_empresa_base.py::test_gastos_financieros_derivan_de_la_deuda_financiera` y `tests/test_evolucion_arquetipo.py::test_tipo_de_interes_implicito_es_razonable_para_el_sector`, reescritos para la fuente nueva |
| 45 | Validación cruzada interna pedida: comparar la cobertura de gastos financieros (BAII/gastos_financieros) generada contra `ratios.cobertura_gastos_fin.huber_9y` del catálogo, sector por sector | **No es una validación limpia — diagnosticado, no ignorado.** El rango generado/huber va de 0,7x a 5,6x según el sector (barrido de 27 sectores × 6 semillas), lejos de "cerca de 1x". Causa: `ratios.cobertura_gastos_fin` usa el MISMO "Gastos financieros" ya diagnosticado como contaminado en `coste_deuda` (#41) — como denominador aquí en vez de numerador, así que hereda el mismo problema desde el otro lado: un sector con gastos financieros reales inflados por partidas ajenas a intereses tendrá una cobertura REAL más baja que la que produce un tipo de interés limpio. Verificado cuantitativamente que esta es la explicación, no una coincidencia: correlación de 0,375 entre el nivel de contaminación de `coste_deuda` de cada sector (su `dato_2024`) y la magnitud del desajuste generado/huber — positiva y en la dirección esperada, aunque moderada (hay más factores en juego, como la dispersión propia de BAII por sector). Se acepta como evidencia débil/consistente con el diagnóstico, no como confirmación fuerte — la validación (a) de #44 (Banco de España) es la que sí ancla el nivel de forma fiable, por ser una fuente genuinamente independiente | Script ad hoc — no se ha añadido como test de regresión por no ser una comprobación con una cota clara (a diferencia de #44) |
| 46 | `TECHO_DELTA_R_ANUAL` (introducido en el encargo de coberturas para acotar `Δtipo_interes` de la fórmula del swap) — ¿sigue haciendo falta con la fuente nueva, o hay que recalibrarlo? | **Ya no se activa nunca — se mantiene igualmente como salvaguarda residual, no se recalibra.** Barrido de 27 sectores × 6 semillas × 3 intensidades del arquetipo "coberturas" (972 transiciones año a año): `Δtipo_interes` máximo observado = **2,34pp** (antes, con la fuente contaminada, el máximo era de ~35pp en sectores "normales") — el techo (3pp) no se activó ni una sola vez (0/972). Se deja el valor tal cual (mismo criterio que otros suelos/techos defensivos del proyecto que rara vez se activan, p. ej. `SUELO_TIPO_INTERES`): coste cero, protege ante un caso futuro genuinamente extremo | Barrido ad hoc (script) |
| 47 | Primer lote de desglose de balance según el mapeo oficial del PGC — perfil de existencias (6 sub-partidas) por categoría de sector, hipótesis de diseño razonada | Mismo patrón que `activo_no_corriente`: 9 categorías, ruido mixto, renormalizado a 1.0. Criterio por categoría: comercio/hostelería con `comerciales` dominante (compran para revender); industria repartida entre materias primas/curso/terminados (más peso en curso/terminados cuanto más largo el proceso); servicios_profesionales/servicios_tic con perfil "casi sin existencias" (concentrado en comerciales/anticipos, sin forzar un reparto de 6 categorías donde apenas hay nada que repartir); construcción con `productos_curso` (obra en curso) dominante y el `anticipos_proveedores` más alto de las 9 categorías (anticipos a subcontratas). Conectado con el arquetipo 5 (exceso de stock) automáticamente: el desglose se recalcula cada año sobre el `existencias` total de ESE año con el perfil fijo, sin código especial | `tests/test_desglose_existencias_periodificaciones.py` |
| 48 | Verificación cuantificada de "combinaciones ilógicas" en el desglose de existencias — pedida explícitamente, no asumida | Redefinida respecto al patrón genérico de `activo_no_corriente` ("el componente dominante nunca cambia"): ese test NO es aplicable aquí sin más porque varias categorías tienen 2-3 componentes deliberadamente próximos por diseño (p. ej. industria 28%/27%/30% entre materias primas/curso/terminados) — con ellos, un test de "dominante nunca cambia" daba 135/486 fallos (27,8%) que NO son combinaciones ilógicas, son el ruido normal alterando cuál de tres valores muy próximos queda primero. Sustituido por parejas semánticas concretas donde la relación de negocio SÍ es clara y nunca debería invertirse (comercio: `comerciales` > cualquier partida de producción; construcción: `productos_curso` > `comerciales`/`subproductos`; industria y administración/educación/sanidad: `comerciales` nunca es la partida mayor) — **0 fallos en 720 comprobaciones** (27 sectores × 8 semillas × 3 años, filtradas por categoría aplicable) | `tests/test_desglose_existencias_periodificaciones.py::test_existencias_sin_combinaciones_ilogicas` |
| 49 | Periodificaciones (activo a corto, pasivo a corto y largo) — ¿son una masa nueva del balance, o se pueden tallar de una masa ya existente sin arriesgar el cuadre? | **Se tallan como fracción de una masa ya existente y ya cuadrada — no una masa nueva.** Periodificación de activo como fracción de `realizable` (el modelo oficial del PGC solo tiene periodificaciones de activo a corto plazo, dentro del grupo que agrega deudores + inversiones financieras c/p + periodificaciones c/p); periodificación de pasivo como fracción de `otras_deudas_corto` Y de `otras_deudas_largo` por separado (el modelo oficial SÍ tiene periodificaciones de pasivo en ambos plazos — confirmado contra la estructura oficial antes de implementar, no asumido). Perfil por categoría (hipótesis de diseño): más alto en sectores con gastos anticipados por seguros/alquileres (administración/educación/sanidad, inmobiliario) del lado de activo, y en sectores con modelo de suscripción/cobro anticipado (servicios_tic, administración/educación/sanidad) del lado de pasivo. Verificado: la periodificación nunca supera su masa madre (0 fallos, `tests/test_desglose_existencias_periodificaciones.py::test_periodificaciones_dentro_de_rango_razonable_y_son_sub_componente`) | `tests/test_desglose_existencias_periodificaciones.py` |
| 50 | Confirmación pedida: ¿el desglose de existencias/periodificaciones se refleja en el cálculo de Fondo de Maniobra/NOF que ya usa el motor? | **El motor NO calcula ningún FM/NOF como magnitud reportada aparte** — solo existe el mecanismo interno de déficit de NOF (`_deficit_y_deuda_corto` en `motor/evolucion_arquetipo.py`), que opera sobre las masas AGREGADAS (`existencias`/`realizable`/`acreedores_comerciales`), no sobre sub-partidas. Como este lote talla sub-detalle DENTRO de masas ya existentes sin cambiar ningún total agregado (verificado: el diff completo del lote no toca `_deficit_y_deuda_corto`/`presion_nof_eur`/`SIGNO_NOF_MASA_CIRCULANTE` en absoluto — 0 líneas modificadas en esas funciones), ese mecanismo queda automáticamente intacto y coherente con el nuevo detalle, sin necesitar ningún cambio | `git diff --stat` del lote (38 inserciones, 0 líneas tocadas en el mecanismo de NOF) |
| 51 | Impacto en la regresión del primer lote de desglose de balance | **0 re-pins — cambio puramente aditivo.** RNG propio e independiente (`rng_desglose_balance`), no desplaza ningún sorteo existente; el desglose es informativo (no cambia ninguna masa agregada del balance, no afecta al cuadre). Suite completa: 561→**567 tests** (6 nuevos), 0 fallos | Suite completa (567 passed) |
| 52 | Precisión posterior del usuario: para sectores de servicios sin producto físico entregable, las existencias no deben ser "mínimas repartidas en 6 categorías" sino marginales/cero de verdad — ¿el criterio original (basado en `categoria_de_sector`) era suficientemente estricto, y aplicaba a los 27 sectores correctos? | **No del todo — verificado contra el dato real de `balance.existencias_pct` del catálogo por SECTOR (no solo por categoría), reveló que la clasificación por las 9 categorías de `activo_no_corriente` no servía para existencias.** Hallazgo clave: dentro de una misma categoría, sectores con carácter de existencias radicalmente distinto — 55.1 Hoteles (0,84% del activo) frente a 47.1 Supermercados (10,75%), ambos en "comercio_hosteleria"; 68 Inmobiliario (13,79%, de los más altos del catálogo) frente al supuesto de "existencias marginales" que tenía el diseño original por representar tenencia como inversión. 6 sectores con dato real ya <1,1% del activo (69.2, 85, 52, 70.2, 55.1, 4941) confirman que el criterio del usuario tiene una base empírica real, no solo intuitiva. Reclasificación completa a 8 "tiers" propios de existencias, por sector (no por categoría) — ver `TIER_EXISTENCIAS_POR_SECTOR` | Barrido de los 27 sectores contra `balance.existencias_pct.huber_9y` (script ad hoc) — tabla completa llevada al usuario antes de fijar el diseño |
| 53 | Las 4 decisiones pendientes tras el hallazgo #52, confirmadas por el usuario | (1) **71 (arquitectura/ingeniería) y 72 (I+D) se suman a 69.2/70.2/62 en el tier "producto_en_curso"** — mismo modelo de negocio (por proyecto/encargo), y el dato real lo respalda: 71 en 5,73% es incluso más alto que 62 (2,43%), consistente con que el trabajo en curso no facturado genera un saldo real. (2) **68 Inmobiliario mantiene una composición REAL, no forzada a cero** — el CNAE 68 agregado mezcla promoción (existencias reales de terrenos/edificios en construcción y terminados) con arrendamiento puro (sin existencias); perfil dominado por `productos_curso` (45%) + `productos_terminados` (30%), misma lógica que `construccion`, documentando explícitamente que el agregado ACCID reflate el peso real de la promoción. (3) **86.1 Sanidad recibe una composición pequeña pero real** (dominada por `materias_primas` al 55%, consumibles/material sanitario fungible), separada de 85 Educación pese a compartir categoría de `activo_no_corriente` — el dato real diverge ~4x (1,69% vs. 0,42%), no es ruido. (4) **Resto sin cambios** — industria, servicios_industriales (ya sin 71/72), comercio_hosteleria (ya sin 55.1) y construcción mantienen los perfiles ya diseñados, incluidas las cifras que salieron distintas de la intuición inicial (35.1 Energía más bajo que el resto de industria, 56.1 Restaurantes más bajo de lo asumido — el dato real manda, no se fuerza nada) | `tests/test_desglose_existencias_periodificaciones.py` — 8 tests, uno por invariante semántica por tier (incluido el nuevo tier `sanidad_consumibles`/`inmobiliario_mixto`), 0 fallos. Suite completa: 569 passed (2 tests nuevos sobre el diseño anterior) |
| 54 | Segundo lote de desglose de balance — Deudores comerciales (7 sub-partidas oficiales, carve-out de `realizable`): ¿perfil por sector/categoría (como existencias) o un único perfil? | **Perfil ÚNICO, sin excepción sectorial — verificado, no asumido.** `ratios.cobro_dias` del catálogo comparado contra los "días implícitos" de `realizable_pct` combinado con `rotacion_activo` (`dias_implicitos = realizable_pct/100/rotacion_activo×365`): ratio cobro_dias real / días implícitos entre **0,89 y 1,13 en los 27 sectores, sin excepción** — "Clientes" ES, esencialmente, la totalidad de `realizable` en todo el catálogo, a diferencia de existencias (lote 1), donde el mismo tipo de verificación reveló divergencias sectoriales fuertes. `PERFIL_DEUDORES_BASE`: `clientes=88%`, resto (deudores varios/personal/activos por impuesto corriente/otros créditos AAPP/accionistas por desembolsos exigidos) marginal, `clientes_empresas_grupo=0%` en el caso base (solo se activa vía arquetipo 20, ver #56) | `tests/test_desglose_deudores_acreedores_grupo.py::test_clientes_domina_con_claridad_en_los_27_sectores` |
| 55 | Segundo lote — Acreedores comerciales (7 sub-partidas, carve-out de `acreedores_comerciales`): ¿se puede anclar a `ratios.pago_dias` del catálogo, igual que deudores se ancló a `cobro_dias`? | **No — mismo patrón de distorsión que `ratios.coste_deuda` (#41), verificado antes de diseñar el perfil, no asumido por analogía con deudores.** `ratios.pago_dias` comparado contra los "días implícitos" de `acreedores_comerciales_pct` + `rotacion_activo`: divergencia de **1,1x a 10,2x** según el sector (peor en 69.2/85/55.1 — los mismos sectores "casi sin producto físico" ya identificados en el lote 1) — el denominador de `pago_dias` (compras/consumos de explotación) es minúsculo en sectores de servicios, dispara el ratio sin que signifique un plazo de pago real más largo, exactamente el mecanismo de distorsión de `coste_deuda`. **Decisión: no usarlo como ancla literal, solo su dirección cualitativa** (ya reconocida como criterio válido en #41-44) — perfil por las 9 categorías de `CATEGORIA_SECTOR` (`PERFIL_ACREEDORES_POR_CATEGORIA`), con `proveedores` dominante en todas pero su peso relativo frente a "Personal (remuneraciones pendientes)" variando según intensidad de mano de obra (dato SÍ fiable: `gastos_personal_pct` del catálogo) — proveedores 52-82% según categoría, personal 3,5-16%, con anticipos de clientes más alto en construcción/administración-educación-sanidad/TIC (cobro anticipado habitual, mismo criterio ya usado en periodificaciones de pasivo, #49). `proveedores_empresas_grupo=0%` en el caso base | `tests/test_desglose_deudores_acreedores_grupo.py::test_proveedores_domina_y_personal_pendiente_es_mayor_en_sectores_intensivos_en_mano_de_obra` |
| 56 | Clasificación de las 5 operaciones del arquetipo 20 ("Operaciones vinculadas") — propuesta obligatoria antes de tocar código, ver docstring de `motor/evolucion_arquetipo.py` (bloque "Operaciones vinculadas") | Revisado el texto literal de cada una de las 5 plantillas para determinar naturaleza (comercial/financiera) y dirección (activo/pasivo) — no asumido por el nombre genérico "operaciones vinculadas": (0) "la sociedad MANTIENE con su matriz un préstamo intragrupo" → la sociedad PRESTA, financiera-ACTIVO → "Inversiones en empresas del grupo y asociadas a l/p" (masa nueva). (1) "ha FACTURADO servicios de gestión a una sociedad del grupo" → comercial-ACTIVO → "Clientes, empresas del grupo" (carve-out de deudores). (2) "SATISFACE a un socio/administrador una renta de arrendamiento" → un socio/administrador NO es necesariamente "empresa del grupo" en sentido PGC (criterio explícito del encargo) → "Acreedores varios" (carve-out de acreedores, NO la línea de grupo). (3) "ha RECIBIDO financiación de una sociedad del grupo" → financiera-PASIVO → "Deudas con empresas del grupo y asociadas a l/p" (masa nueva); el propio texto excluye gastos financieros adicionales, así que esta masa NUNCA entra en `deudas_fin_largo/corto` (las únicas que alimentan `gastos_financieros = deuda_financiera_media × tipo_interes`). (4) "la matriz ha PRESTADO servicios de asistencia técnica" → la sociedad RECIBE el servicio, lo debe → comercial-PASIVO → "Proveedores, empresas del grupo" (carve-out de acreedores). Clasificación aprobada por el usuario antes de implementar | `motor/evolucion_arquetipo.py::TIPO_OPERACION_POR_INDICE`/`COMPONENTE_DESGLOSE_POR_TIPO`; `tests/test_desglose_deudores_acreedores_grupo.py::test_cada_uno_de_los_5_tipos_aterriza_en_la_linea_oficial_correcta` |
| 57 | Mecánica temporal de las 2 operaciones FINANCIERAS del arquetipo 20 (préstamo a matriz / financiación recibida de grupo) — ¿evento discreto en un año fijo (como la adquisición del arquetipo 18) o perfil continuo desde que el arquetipo se activa? | **Perfil continuo — "% fijo de la magnitud de referencia, recalculado cada año", mismo patrón ya establecido en todo el proyecto (existencias/periodificaciones del lote 1), no un mecanismo nuevo.** Se descartó el evento discreto (que habría exigido replicar la maquinaria de AÑO_ADQUISICION) porque el patrón continuo ya resuelve las 2 exigencias del encargo sin código nuevo: (a) genera un delta real año a año (2024 Y 2025, no solo un año de inyección) que la reconciliación del EFE debe explicar — más exigente que un único evento, no menos; (b) al recalcularse sobre la magnitud de referencia del año ANTERIOR (evita la circularidad de depender de un patrimonio_neto/deuda financiera que ese mismo año todavía no existe, mismo criterio que el resto de bases "proporcionales" de `_evolucionar_un_año`), la masa se pliega en `activo_no_corriente_eur`/`otras_deudas_largo_eur`/`disponible_proporcional_eur` ANTES del chequeo de endeudamiento — que por tanto la ve SIEMPRE, en los 2 años, no solo en un año de inyección puntual. Techo defensivo `TECHO_FRACCION_DISPONIBLE_PRESTAMO_MATRIZ=0,9` (nunca deja `disponible` negativo) — interactúa raramente con `SUELO_IMPORTE_VINCULADAS_EUR` (2/324 casos en el barrido de plausibilidad, documentado como excepción aceptada en el propio test: el techo de cuadre gana sobre el suelo narrativo cuando la caja disponible es escasa, el importe citado sigue siendo el real del balance, nunca inventado) | `tests/test_desglose_deudores_acreedores_grupo.py::test_financiacion_recibida_de_grupo_entra_en_el_calculo_de_endeudamiento`, `test_riesgo_endeudamiento_se_activa_igual_que_en_apalancamiento_al_combinar_con_9`; `tests/test_memoria.py::test_operaciones_vinculadas_importe_dentro_de_cotas_de_plausibilidad` |
| 58 | Confirmación del usuario antes de implementar (punto 2): "el EFE ya tiene una línea propia para 'empresas del grupo' dentro de financiación (C.10)" — ¿correcto? | **No — la premisa no coincidía con el código actual, verificado con una relectura completa de `motor/efe.py` antes de aceptarla (mismo criterio de nunca cumplir silenciosamente una premisa que contradice el código: ver hallazgos #33 "gran empresa"/#27 fondo de comercio).** Solo existía UN campo agregado `c10_variacion_neta_deuda_financiera` (`deudas_fin_largo+deudas_fin_corto`, sin desglose a/b/c/d oficial). Corrección aplicada, no solo señalada: 2 líneas nuevas — `b6c_prestamo_empresas_grupo` (Sección B, variación de `inversion_grupo_largo_eur`, junto a `b6a_empresas_grupo_adquisicion` del arquetipo 18, misma línea oficial B.6 "Empresas del grupo y asociadas") y `c10c_empresas_grupo` (Sección C, letra (c) del desglose oficial de la línea 10, variación de `deuda_grupo_largo_eur`) — **deliberadamente FUERA de `c10`**, que debe seguir representando solo deuda con entidades de crédito (coherente con que esa masa nunca genera gastos financieros, confirmado en el texto de la propia nota de memoria). Ambas excluidas de `a3f`/`delta_activo_no_corriente_organico` (Sección A/B genéricas), mismo criterio de exclusión ya usado para grupo89 (#35) | `motor/efe.py`; `tests/test_desglose_deudores_acreedores_grupo.py::test_efe_cuadra_y_usa_las_lineas_propias_con_cada_uno_de_los_5_tipos` |
| 59 | Verificación exhaustiva final del segundo lote: regresión completa (lote 1, amortización, grupo 8/9, tipo de interés de mercado, EFE/ECPN), 0 re-pins, y reconciliación con los mecanismos existentes que también mueven caja real (adquisición, cobertura/subvención) | **0 descuadres, 0 re-pins en toda la suite previa.** RNG propio e independiente (`rng_desglose_balance_lote2` en `generar_empresa_base`; sorteo de operación vinculada trasladado a `motor.evolucion_arquetipo._sortear_operacion_vinculada`, misma secuencia exacta que usaba antes `motor.memoria` — verificado que el índice de plantilla NUNCA depende de `etiquetas_ya_usadas` para este arquetipo: las 5 plantillas comparten etiqueta fija, así que la resolución de colisiones siempre devuelve el primer elemento del `rng.permutation` barajado, sin ninguna rama alternativa que pudiera desincronizar nota↔balance). Combo E completo (adquisición + activo_mantenido_venta + operaciones_vinculadas + coberturas) sigue cuadrando con las 2 masas nuevas conviviendo con adquisición/cobertura/subvención. Suite completa: 569→**584 tests** (16 nuevos en `tests/test_desglose_deudores_acreedores_grupo.py`, más ajustes de firma sin cambio de comportamiento en `tests/test_memoria.py`/`tests/test_combinacion_arquetipos.py`/`tests/test_arquetipos.py` por la promoción del arquetipo 20 a `clase="cuantitativo"`), 0 fallos | Suite completa (584 passed) |
| 60 | Tercer lote de desglose de balance — Provisiones a largo/corto plazo (subgrupo 14 del PGC + 4994/4999, `motor/provisiones.py`), epígrafes B.I/C.II hoy ausentes del motor. Interacción detectada al inyectar dotación/exceso en la PyG (post-hoc, mismo patrón que grupo89): puede empujar `margen_bruto_pct`/`baii_pct` fuera del techo/suelo ya contenido por `_limitar_por_subtotal`/`_limitar_gastos_personal_por_baii` (esos chequeos actúan ANTES de que la provisión se inyecte, igual que ya pasaba con la imputación de subvención, #39) — y una provisión de categoría 140 (retribuciones al personal) puede subir `gastos_personal_pct` por una razón ajena por completo a la contención de `mejora_ebitda` (10) | **Mismo criterio que #39, ampliado a los 2 casos nuevos.** 3 tests preexistentes (`test_margen_bruto_nunca_supera_el_techo_del_sector`, `test_mejora_ebitda_baii_respeta_el_techo_de_plausibilidad_del_sector`, `test_mejora_ebitda_las_subidas_de_gastos_personal_siempre_coinciden_con_el_limite_por_ruido`) ampliaron su exclusión ya existente (antes solo `subvencion_transferencia_bruto_eur>0`) para excluir también los ejercicios donde la provisión mueve algo ese año (`provision_dotacion_eur>0`/`provision_exceso_eur>0`, o específicamente dotación de categoría 140 para el tercer test) — **0 re-pins, 0 valores numéricos modificados**, solo la condición de exclusión ampliada, exactamente la misma naturaleza de interacción ya documentada y aceptada. **Hallazgo positivo, no buscado**: la dotación/exceso de provisión, a diferencia de grupo89 (cobertura/subvención, pura valoración sin caja real detrás), SÍ tiene siempre una contrapartida real en `otras_deudas_largo`/`otras_deudas_corto` — así que el EFE la reconcilia EXACTAMENTE mediante las líneas A.3.e/A.3.f ya existentes, sin necesitar ninguna línea nueva (verificado: 0 descuadres en 200 EFE generados con provisión activa, ver `tests/test_provisiones.py::test_efe_cuadra_con_provision_activa_sin_lineas_nuevas`) | `tests/test_provisiones.py`; exclusiones ampliadas en `tests/test_arquetipos_generalizados.py` |
| 61 | Probabilidad de fondo de la provisión (independiente de cualquier arquetipo, mismo patrón que `sortear_subvencion_baseline`) — pedida explícitamente en el rango 20%-30%, fácil de encontrar por búsqueda de semilla, y verificada con el arquetipo 6 ("Aumento de clientes (base)", línea base sana), no solo con el valor teórico | **26,4% observado** en un barrido de 216 casos (27 sectores × 8 semillas, arquetipo 6 en solitario) — dentro del rango pedido, sin necesitar probar decenas de semillas para encontrar un caso con provisión activa. Las 8 categorías incluidas (140/141/142/143/145/146/4994/4999) aparecieron TODAS en un barrido más amplio (27 sectores × 20 semillas); 147 (descartada explícitamente, ver docstring de `motor/provisiones.py`) nunca aparece, por construcción — no está en `CATEGORIAS_PROVISION`. Multiplicadores sectoriales verificados cuantitativamente, no solo por construcción del peso: 143 (desmantelamiento) más frecuente en industria/construcción que en el resto (verificado con conteos reales, no solo el multiplicador teórico); 4994 (contratos onerosos) más frecuente en sectores "por proyecto" (tier `producto_en_curso` del lote 1) + construcción; 4999 (garantías) más frecuente en industria + comercio/hostelería | `tests/test_provisiones.py` (`test_probabilidad_de_fondo_con_arquetipo_6_dentro_del_rango_pedido`, `test_las_8_categorias_aparecen_en_barrido_amplio`, `test_categoria_147_no_esta_incluida`, y los 3 tests de plausibilidad sectorial 143/4994/4999) |
| 62 | Largo vs. corto plazo de la provisión — confirmado contra el PGC que el subgrupo 529 no introduce categorías nuevas (es una reclasificación del mismo catálogo 140-146/4994/4999) — ¿qué mecanismo usar para el paso natural largo→corto, dado que `EfectoReclasificacionDeuda` ancla su continuidad a un ratio de catálogo (`ratios.calidad_deuda`) que no existe para provisiones? | **Horizonte determinista por instancia, no continuidad-vs-huber — mismo ESPÍRITU que `EfectoReclasificacionDeuda` (mover saldo entre plazos sin alterar el total), mecanismo distinto por necesidad (sin ratio de catálogo al que anclarse).** Cada provisión sortea `plazo_total_años` (rango 1,5-4,0 años) UNA vez, en el momento de la dotación; cada año se reclasifica ÍNTEGRA (nunca repartida entre largo y corto a la vez) según si el horizonte restante supera o no 1 año. Verificado en barrido (27 sectores × 15 semillas): transiciones largo→corto observadas en la práctica, no solo posibles en teoría (`test_reclasificacion_largo_a_corto_ocurre_en_barrido_amplio`) — ejemplo concreto llevado al usuario: sector 29 (industria), categoría 143 (desmantelamiento), dotada en 2024 (127.375€, 100% largo plazo, sin efecto en Fondo de Maniobra ese año) — en 2025 el horizonte restante cae por debajo de 1 año, el saldo remanente (47.075€, tras aplicación 56.210€ + exceso 24.090€) se reclasifica ÍNTEGRO a corto plazo, reduciendo el FM de ese año en exactamente ese importe (verificado con el contrafactual: FM sin la provisión habría sido 47.075€ mayor) | `tests/test_provisiones.py::test_reclasificacion_largo_a_corto_ocurre_en_barrido_amplio`, `test_saldo_nunca_negativo_y_nunca_largo_y_corto_a_la_vez` |
| 63 | Independencia frente al arquetipo 22 ("informacion_relevante_memoria", tema "contingencia legal") — el encargo distingue explícitamente PROVISIÓN (obligación probable y estimable, SÍ en balance) de CONTINGENCIA (obligación posible o no estimable, NO en balance, solo memoria) y pide no mezclarlos aunque puedan coexistir sin relación causal | **Confirmado, verificado no solo por diseño.** El arquetipo 22 no se tocó en absoluto en este lote (0 líneas modificadas en su código); combinado con el 6 (`generar_caso_combinado`), el saldo/movimiento de la provisión es IDÉNTICO al que produce el mismo caso en solitario (verificado numéricamente, no solo "no debería cambiar" — `pytest.approx(..., abs=0.01)` sobre `provision_saldo_largo_eur`/`..._corto_eur` en 30 semillas), y el 22 sigue generando su nota puramente textual sin ninguna huella de balance, coexistiendo con la provisión sin ninguna relación causal entre ambos | `tests/test_provisiones.py::test_arquetipo_22_sigue_sin_tocar_el_balance_combinado_con_provision` |
| 64 | Verificación exhaustiva final del tercer lote: regresión completa (lotes 1/2, amortización, grupo 8/9, tipo de interés de mercado, EFE/ECPN), sin descuadres ni caja "flotando" sin reflejo | **0 descuadres de balance, 0 descuadres de EFE** en los barridos del lote (ver #60/#61 para las cifras). `disponible` nunca negativo (verificado, mínimo observado 0,0€ — el mecanismo de déficit de NOF ya existente absorbe correctamente la aplicación de provisión cuando coincide con poca caja disponible, sin código nuevo). Suite completa: 584→**600 tests** (16 nuevos en `tests/test_provisiones.py`, 3 exclusiones ampliadas sin re-pin en `tests/test_arquetipos_generalizados.py`), 0 fallos | Suite completa (600 passed) |
| 65 | Pedido explícito de repaso posterior: ¿la identidad Balance↔PyG del resultado del ejercicio (`capital_social + reservas + resultado_ejercicio + ajustes_pn == balance.patrimonio_neto`, misma comprobación ya exigida para amortización, #30/`tests/test_amortizacion.py::_identidad_pn`) queda cubierta con provisiones activas, o solo el total agregado activo=pasivo+PN (`_cuadra`)? | **No estaba cubierta explícitamente — los 600 tests del cierre del lote solo verificaban el total agregado.** `_cuadra` (activo=pasivo+PN) no depende de cómo se derive `reservas_eur`, así que un error en la propagación de la dotación/exceso de provisión hacia `reservas_eur` habría podido pasar desapercibido. Verificado explícitamente (312 ejercicios con provisión activa, 27 sectores × 15 semillas, arquetipo 6): **0 fallos, diferencia máxima 3,7e-9€** (ruido de coma flotante) — la identidad se mantiene EXACTA, por construcción (`reservas_eur` se deriva de `pyg_eur["resultado_ejercicio"]`, que ya incluye el ajuste post-hoc de la provisión, igual mecanismo que grupo89 — nunca dos fuentes de verdad separadas que puedan desincronizarse). Añadidos 2 tests permanentes que reproducen esta comprobación (más EFE/ECPN en solitario Y combinado con el arquetipo 22) — no dependían de re-pin, solo de cobertura que faltaba | `tests/test_provisiones.py::test_identidad_balance_pyg_resultado_ejercicio_con_provision_activa`, `test_efe_y_ecpn_cuadran_con_provision_activa_arquetipo_solo_y_combinado`. Suite completa: 600→**602 tests**, 0 fallos |
| 66 | Cuarto y último lote de desglose de balance — Deudas financieras (largo/corto plazo): clasificación de las 5 categorías oficiales por Tipo, propuesta ANTES de implementar (arrendamiento financiero y obligaciones sin base de catálogo — hipótesis de diseño explícita) | **Aprobado con 1 corrección del usuario** (ver #67). Arrendamiento financiero: Tipo 1, perfil por categoría de sector (mayor en transporte_logistica 30%/construcción 22%/industria 18% — activo material pesado en vehículos/maquinaria, conecta con las cohortes de amortización ya construidas; menor en servicios de oficina, 4-6%) — verificado que ninguna de las 218 columnas del catálogo distingue esta financiación, mismo estatus que otros perfiles ya aceptados sin ancla (`PERFIL_ACTIVO_NO_CORRIENTE_POR_CATEGORIA`). Obligaciones y valores negociables: Tipo 1, casi cero salvo industria (4% — incluye energía/siderurgia, mayor tamaño típico), resto 1%. Otros pasivos financieros: Tipo 1, residual plano 3%. Entidades de crédito: Tipo 1, NUNCA sorteado — es el RESIDUAL de restar las otras 3 al total con coste (dominante por defecto). Derivados: Tipo 2 puro, sin probabilidad de fondo, disparado exclusivamente por el valor razonable del swap del arquetipo 21 | `motor/empresa_base.py` (`PERFIL_DEUDAS_FIN_POR_CATEGORIA`, `FRACCION_LARGO_POR_TIPO_DEUDA_FIN`); `tests/test_desglose_deudas_financieras.py` |
| 67 | Corrección del usuario sobre `reclasificacion_deuda` (arquetipos 8/16): la propuesta inicial dejaba que la reclasificación del agregado se repartiera proporcionalmente entre las 4 categorías Tipo 1 (incluido arrendamiento financiero/obligaciones) — el usuario señaló que el mismo argumento que justifica el Tipo propio de leasing ("calendario fijo por contrato, no se renegocia como un préstamo bancario") es el argumento CONTRA dejar que 8/16 lo mueva | **Corregido antes de comitear, no a posteriori.** `reclasificacion_deuda` sigue operando SIN NINGÚN cambio de código sobre el agregado `deudas_fin_largo_proporcional_eur`/`..._corto_proporcional_eur` (el mecanismo en sí no se tocó) — la exclusión se logra enteramente en el desglose POSTERIOR: "Entidades de crédito" se calcula como RESIDUAL (`calcular_desglose_deudas_fin`, `motor/empresa_base.py`) después de restar el reparto FIJO por caso de arrendamiento financiero/obligaciones/otros pasivos financieros (cada uno con su propia fracción largo/corto, sorteada una vez, `FRACCION_LARGO_POR_TIPO_DEUDA_FIN` — por TIPO de instrumento, no por sector: el calendario de un leasing/bono no varía por sector). **Verificado cuantitativamente, no asumido**: con el arquetipo 16 ("riesgo de refinanciación") empujando agresivamente el agregado hacia corto plazo, la fracción largo/(largo+corto) de arrendamiento financiero/obligaciones/otros se mantiene estable entre 2023 y 2025 (Δ<0,03 en el barrido) mientras la de "Entidades de crédito" se mueve con fuerza (Δ>0,05) — absorbe ella sola el efecto de la reclasificación. **Techo defensivo necesario y verificado**: en sectores/semillas con un draw atípico del catálogo (`deudas_fin_largo` ya casi nulo por el ruido 85%/15% típico/atípico — no relacionado con la reclasificación), el reparto fijo de las 3 categorías podría pedir más "largo" del que existe — `calcular_desglose_deudas_fin` reescala proporcionalmente ENTRE ELLAS para que la suma nunca supere el total real (nunca dos categorías nuevas: solo cambia SU PROPIO reparto largo/corto ese año, conservando cada categoría su total). Verificado en 648 ejercicios de estrés (27 sectores × 8 semillas × 3 años, arquetipo 16): 0 casos con "Entidades de crédito" negativo, 0 descuadres — el techo se activa (residual <1.000€) en 57/648 | `tests/test_desglose_deudas_financieras.py::test_reclasificacion_deuda_no_mueve_el_reparto_propio_de_leasing_obligaciones_otros`, `test_entidades_credito_nunca_negativo_bajo_reclasificacion_agresiva` |
| 68 | Migración del derivado del arquetipo 21 (cobertura de flujos de efectivo) desde la aproximación anterior ("otros activos financieros" cuando es activo / "otras deudas" cuando es pasivo, ver #34-36) a su línea propia "IV. Derivados" dentro de "Deudas financieras a largo plazo" (solo el lado PASIVO — el lado activo queda fuera del alcance de este lote, sigue en `activo_no_corriente`, identificable vía `cobertura_valor_swap_eur`) — ¿se puede reubicar sin romper el interés, el endeudamiento o el EFE/EIGR ya validados? | **Sí, verificado explícitamente antes y después de implementar, no solo argumentado.** El riesgo real (confirmado leyendo el código, no asumido): `deudas_fin_largo_eur`/`deudas_fin_corto_eur` alimentan directamente `gastos_financieros = deuda_financiera_media × tipo_interés` — si el derivado se pliega ahí sin más, una valoración a mercado negativa generaría "interés" a la tasa media del sector, un error económico. **Solución**: el derivado se añade a `deudas_fin_largo_eur` SOLO dentro de `_construir_balance` (para el balance reportado), nunca antes de calcular `deuda_financiera_media_eur` (que sigue usando exclusivamente la base "con coste") — mismas exclusiones aplicadas en cascada donde `anterior.balance_eur["deudas_fin_largo"]` se usaba como base proporcional/de inicio (`deudas_fin_largo_proporcional_eur`, `deuda_financiera_inicio_eur`), para que el derivado del año anterior tampoco se componga como si fuera principal. **Endeudamiento**: `_endeudamiento` opera sobre `pasivo_no_corriente`/`pasivo_corriente` ya agregados — mover euros entre `deudas_fin_largo` y `otras_deudas_largo` (su ubicación anterior) no cambia ese total, confirmado sin tocar el mecanismo. **EFE**: la exclusión que antes vivía en `a3f` (porque el derivado-pasivo vivía en `otras_deudas_largo`) se trasladó a `c10` (que ahora excluye el derivado antes de calcular la variación neta de deuda financiera) — mismo importe excluido, solo cambia qué fórmula lo resta; **0 cambios en ningún total de EFE/ECPN ya validado** (confirmado: los 602 tests preexistentes pasaron sin modificar NINGÚN valor de referencia tras la migración). **EIGR**: no necesitó ningún cambio — no referencia `balance_eur` ni los campos de colocación del derivado en absoluto, solo los flujos brutos ya expuestos (`cobertura_eficaz_bruto_eur` etc.), confirmado leyendo el código antes de asumirlo | `tests/test_desglose_deudas_financieras.py` (`test_derivado_pasivo_aterriza_en_deudas_fin_largo_no_en_otras_deudas`, `test_tipo_interes_no_se_ve_afectado_por_el_derivado_pasivo`, `test_efe_cuadra_con_derivado_pasivo_y_activo`, `test_eigr_y_ecpn_siguen_cuadrando_y_coincidiendo_con_derivado_activo`); suite completa 602 tests preexistentes, 0 modificados |
| 69 | Verificación exhaustiva final del cuarto y último lote: regresión completa (lotes 1/2/3, amortización, grupo 8/9, tipo de interés de mercado, EFE/ECPN), identidad Balance↔PyG explícita (lección del lote 3), Combo E con el derivado reubicado | **0 descuadres, 0 re-pins en los 602 tests preexistentes.** Identidad Balance↔PyG (`_identidad_pn`, misma fórmula que #65) verificada explícitamente con derivado activo (pasivo y activo) y en barrido amplio sin cobertura (27 sectores × 3 arquetipos que mueven deuda financiera) — 0 fallos. Combo E completo (adquisición + activo_mantenido_venta + operaciones_vinculadas + coberturas) sigue cuadrando (balance, identidad PN, EFE, ECPN) con el derivado en su línea nueva, conviviendo con las demás masas que también mueven caja real de lotes anteriores. Con este lote se completa el desglose granular de balance/PyG según el modelo oficial del PGC — los 4 lotes juntos: existencias/periodificaciones (#47-53), deudores/acreedores/empresas del grupo (#54-59), provisiones (#60-65), deudas financieras/derivados (#66-69). Suite completa: 602→**621 tests** (19 nuevos en `tests/test_desglose_deudas_financieras.py`), 0 fallos | Suite completa (621 passed) |
| 70 | Validación de plausibilidad del caso completo (sección 2.13) — diseño propuesto ANTES de implementar: qué hacer con un ratio fuera de rango (diagnóstico vs. corrección), qué ratios merecen palanca nueva, cuándo se ejecuta, cómo interactúa con las señales ya existentes. Pedido explícito del usuario: revisión SISTEMÁTICA de los 27 (25 `ratios.*` + baii_pct/margen_bruto_pct) buscando circularidad (algo sorteado directamente del Huber, no derivado), no solo el caso encontrado por casualidad | **Diagnóstico puro, sin corrección nueva, aprobado tal cual.** Ninguno de los 25 tiene palanca de amortiguación natural fuera de las ya existentes — el catálogo no tiene ratios granulares por sub-partida, así que los 4 lotes de desglose no aportan ninguna palanca nueva sobre estos ratios agregados. Ejecutada al final de `generar_evolucion_combinada` (cubre también `generar_caso_combinado`, que la envuelve). **Revisión sistemática de circularidad — encontrados 2, no solo 1:** `ventas_empleado` (circular en los 3 años — `estimar_plantilla` define plantilla = cifra_negocio/ventas_empleado_sorteado, recalcularlo reproduce exacto el sorteo) y **`rotacion_activo`** (circular SOLO en el año base 2023 — no detectado en la primera pasada del usuario, encontrado al aplicar la revisión sistemática pedida: `generar_empresa_base` sortea `rotacion_activo` y calcula `activo_total = ventas/rotacion_activo_sorteado`, el plug de cuadre no toca activo; en 2024/2025 no se vuelve a sortear, así que sí es comprobable) | `motor/evolucion_arquetipo.py` (docstring del bloque "Validación de plausibilidad del caso completo") |
| 71 | Verificación de las 27 fórmulas contra el PDF ACCID (`docs/ratios2024.pdf`) antes de correr el stress test — mismo estándar ya aplicado a `coste_deuda` (#41), no fórmulas inventadas ni asumidas de la lógica interna del motor | **3 errores propios encontrados y corregidos ANTES del stress test** (un primer smoke test mostró desviaciones de miles de MAD, señal inequívoca de bug, no de hallazgo real): (1) los 3 ratios "por empleado" (ventas/beneficio/gastos de personal) están en MILES de €, no en € crudos — confirmado por el propio orden de magnitud del huber_9y (651, 61, 22 — imposible en € crudos "por empleado"). (2) `financiacion_clientes` NO es una cifra en días — el PDF la define como "Acreedores comerciales / Clientes", un ratio directo (típicamente 0,3-1,5). (3) `capacidad_devolucion` usa el TOTAL de deudas (todo el pasivo) en el denominador, no solo la deuda financiera — el propio texto del PDF explica que sustituye "Préstamos" por "total de deudas" al no poder aislarlos en un balance abreviado real. **2 discrepancias encontradas sobre mecanismos YA existentes y cerrados — traídas al usuario, NO corregidas en este encargo, mismo criterio que `coste_deuda`:** `rotacion_existencias` (PDF: Consumos/Existencias; el ancla de `masa_circulante` para los arquetipos 1/5 usa Ventas/Existencias desde su propio encargo, ya cerrado) y `calidad_deuda` (PDF: Pasivo corriente/Deudas totales; `reclasificacion_deuda` -8/16- usa deudas_fin_largo/deuda_financiera — ámbito distinto, polaridad distinta). Esta pasada usa la fórmula CORRECTA del PDF en ambos casos (necesario para que la comprobación contra el Huber del catálogo tenga sentido) — el código de los mecanismos antiguos no se tocó, decisión pendiente de un encargo futuro si se quiere alinear | `motor/evolucion_arquetipo.py::_ratios_derivados_del_caso`; `tests/test_plausibilidad_caso.py` (3 tests de regresión sobre las fórmulas corregidas) |
| 72 | Verificación cuantificada del punto 4 (superconjunto, "no las duplica ni las contradice") — el stress test reveló que la primera implementación NO era un superconjunto real: casos con `riesgo_endeudamiento=True`/`riesgo_plausibilidad_pyg=True` sin señal nueva correspondiente | **2 causas raíz encontradas y corregidas, ninguna es un hallazgo de negocio — ambas son de la propia mecánica de comparación:** (1) `ratios.endeudamiento` comparaba contra `huber+3·MAD` sin recortar, mientras la contención ya existente usa `min(huber+3·MAD, TECHO_ENDEUDAMIENTO_MAXIMO_ABSOLUTO=0,85)` — un sector con `huber+3·MAD>0,85` podía activar `riesgo_endeudamiento` (que SÍ aplica el recorte) sin activar la señal nueva (que no lo aplicaba) — corregido, `ratios.endeudamiento` ahora usa el mismo techo exacto. (2) Comparación ESTRICTA (`>`/`<`) — las contenciones ya existentes (`_endeudamiento`, `_limitar_por_subtotal`, `_limitar_gastos_personal_por_baii`) corrigen ANALÍTICAMENTE hasta dejar el valor EXACTO en su propio techo/suelo; el ruido de coma flotante de esa corrección (convergencia iterativa en euros para endeudamiento, aritmética "100−techo" para la PyG) deja el valor final 1e-11 a 1e-13 por DEBAJO del límite exacto — con `>` estricto, un caso "corregido con éxito hasta el límite" no quedaba señalizado. Corregido con comparación INCLUSIVA (`>=`/`<=`) más una tolerancia `EPSILON_DESVIACIONES_PLAUSIBILIDAD_CASO=1e-6` (en unidades de MAD). **Excepción documentada, no un fallo, encontrada al verificar:** cuando una provisión (tercer lote) dota/libera el MISMO año que `mejora_ebitda`/`mejora_margen` actúan sobre baii/margen_bruto, la dotación se inyecta DESPUÉS de que la contención de PyG ya fijó el valor en su límite — un ajuste transversal posterior e independiente puede devolver el valor final a un rango plausible, mismo patrón ya aceptado en #39/#60 (la interacción de provisión/subvención con la contención de PyG). Verificado tras las 2 correcciones: 0 discrepancias restantes en el barrido completo (salvo los casos de provisión coincidente, ya documentados como excepción esperada) | `tests/test_plausibilidad_caso.py::test_riesgo_endeudamiento_implica_señal_de_ratios_endeudamiento`, `test_riesgo_plausibilidad_pyg_implica_señal_de_baii_o_margen_bruto` |
| 73 | Stress test cuantificado obligatorio: 27 sectores × 2 segmentos × 4 semillas, arquetipos en solitario (leve/moderado/fuerte) + las 6 combinaciones recomendadas — % de casos con al menos 1 señal por ratio, confirmación de que el hallazgo original (baii_pct/margen_bruto_pct, #18) queda señalizado, y cualquier ratio con tasa sorprendentemente alta traído sin arreglar | **Resultados (12.960 casos generados, 38.880 ejercicios): 99,3% de los casos tienen al menos 1 ratio señalizado; por ratio, entre 3,3% (`pyg.margen_bruto_pct`) y 59,5% (`ratios.liquidez`); overlap con `riesgo_endeudamiento` 99,92% (tras la excepción de `contencion_al_limite`, ver más abajo) y con `riesgo_plausibilidad_pyg` (solo config 'leve') 100% tras excluir los años con provisión activa.** **Cierre confirmado del hallazgo #18** (no por comparación directa de porcentajes — metodologías distintas, ver nota): usando `aumento_clientes:leve` como el mismo proxy "sin empuje" que #18, la señal ANTIGUA (`riesgo_plausibilidad_pyg`) da **0,00%** (648 ejercicios) — nunca se activa porque ese arquetipo no toca ninguna primitiva de baii/margen_bruto, exactamente el vacío de detección que #18 documentó — mientras que la señal NUEVA (Huber±3·MAD sobre baii_pct/margen_bruto_pct ya derivados del caso) sí detecta un 20,8% de esos mismos ejercicios: lo que antes NO se detectaba nunca, ahora se detecta. *(Nota metodológica: la cifra 21,54% del script ad hoc usa "leve" de los 17 arquetipos cuantitativos como proxy conjunto — incluye archetypes que SÍ empujan baii/margen_bruto incluso en "leve" tras la recalibración de intensidad de #20 (`mejora_ebitda`, `mejora_margen`, `capex_elevado`, `resultado_extraordinario`, `adquisicion`), así que no es comparable directamente al 6,9% de #18, que usaba un único arquetipo a intensidad≈0. La comparación correcta y limpia es la de arriba, con `aumento_clientes` aislado.)* **Segunda excepción de superconjunto encontrada y confirmada (nueva, no cubierta por #72):** en combinaciones con varios arquetipos 'fuerte' apilados (señaladamente el combo F), 23-31 ejercicios mostraron `riesgo_endeudamiento=True` sin señal nueva — depurado caso a caso: TODOS tenían `contencion_al_limite=True` **con** `deterioro_aplicado_eur>0` (la palanca de amortiguación de circulante existía, se agotó en una sola pasada del bucle sin reevaluar el resultado, y el efecto secundario de esa pasada — menos deuda nueva financiando el circulante, luego menos gasto financiero, luego más PN — deja el endeudamiento FINAL por DEBAJO del techo pese a haber tocado techo el candidato sin corregir). Mismo patrón que la excepción de provisión/PyG: señal sobre el PROCESO de contención, no sobre el valor final — documentada y excluida en el test de regresión, no corregida (no es un fallo del mecanismo de contención, que hace exactamente lo que su propio docstring describe). **Hallazgo MAYOR, traído sin arreglar (mismo criterio que `coste_deuda`, NO decidido en este encargo):** la tasa de señalización es alta incluso SIN NINGÚN arquetipo empujando la magnitud comprobada — verificado de forma aislada: `apalancamiento:leve` (no toca activo/pasivo corriente en absoluto) da 18,75% de señales en `ratios.liquidez`; `aumento_clientes:leve` (no toca ninguna primitiva de PyG) da 20,8% en baii_pct/margen_bruto_pct. Osea: el ruido de la propia generación de la base (`motor/empresa_base.py::_generar_partida`, primitivas independientes con hasta 3,0σ de cola atípica, `PROB_ATIPICO=0,15`) combinado en un ratio derivado produce más variación que la que el Huber/MAD REAL del sector (calculado sobre datos ACCID) tolera a 3 desviaciones — es decir, la pasada de plausibilidad no está mal calibrada: está exponiendo por primera vez que el generador de base simula primitivas más independientes entre sí (menos correlacionadas) que las empresas reales del sector, cuyos componentes de balance/PyG se mueven más juntos. El patrón de valores extremos observado (56.826 señales con >8 desviaciones, concentradas en combinaciones como sector "24.1" + `crecimiento_destruccion_caja:leve` sobre `liquidez`/`plazo_existencias`/`cobro_dias`, hasta ~37 desviaciones) es consistente con esta misma causa, amplificada en sectores cuyo MAD histórico real es particularmente estrecho (p. ej. `ratios.liquidez` en el sector 24.1: MAD = 7,4% del huber, frente al 11,7% del sector 62). **No se ha tocado nada de `motor/empresa_base.py` en este encargo** — es un hallazgo estructural sobre el generador de base, de un alcance mayor que esta pasada de validación, pendiente de decisión conjunta sobre si (y cómo) abordarlo: ¿correlacionar las primitivas al generarlas, ampliar el criterio de plausibilidad para el ruido combinado, u otra vía? | Script ad hoc (`stress_plausibilidad.py`, no un test permanente); `tests/test_plausibilidad_caso.py::test_riesgo_endeudamiento_con_contencion_al_limite_puede_no_señalizar` (regresión de la 2ª excepción) |
| 74 | Antes de aceptar el hallazgo mayor de #73 como estructural: decidir los 2 hallazgos de fórmula de #71 (`rotacion_existencias`, `calidad_deuda`), no dejarlos "traídos sin decidir" — si el mecanismo de un arquetipo mueve un ratio con una fórmula distinta a la que ACCID usó para construir su Huber/MAD, la pasada de plausibilidad compara magnitudes distintas, lo que podría inflar señales sin ser ruido genuino del simulador | **`rotacion_existencias` — corregido.** `_masa_circulante_objetivo` (arquetipos 1/3/5, `formula="rotacion"` sobre existencias) usaba Ventas/Existencias; ahora usa Consumos de explotación/Existencias (consumos de ESTE año aún no existen en este punto del pipeline — se estiman proporcionales al mismo crecimiento ya aplicado a `ventas`, igual criterio que el resto de masas no tocadas por el arquetipo). Verificado sin efectos secundarios: existencias sigue creciendo de forma monótona con la intensidad (leve<moderado<fuerte), sin saturar ni degenerar, y las 3 categorías de la suite (`test_evolucion_arquetipo.py`, `test_arquetipos_generalizados.py`, `test_combinacion_arquetipos.py`) pasan sin tocar ningún valor de referencia. **`calidad_deuda` — intentado y REVERTIDO, no corregido.** Se implementó la versión fiel a ACCID (resolver qué reparto largo/corto de deuda financiera lleva el pasivo corriente TOTAL — no solo deuda financiera — al `ratios.calidad_deuda` objetivo, con la polaridad invertida frente al ancla anterior para conservar la narrativa de "mejora"/"empeora" de los arquetipos 8/16) y se probó contra la suite: **rompe el mecanismo por saturación estructural.** La deuda financiera es una palanca demasiado pequeña frente al pasivo total en los sectores probados (huber_calidad_deuda≈0,73 — la mayoría del pasivo corriente real es acreedores/otras deudas, no deuda financiera): CUALQUIER intensidad, incluida "leve", saturaba el reparto al límite estructural (deudas_fin_largo=0 o deudas_fin_corto=0) en 14-15 de 15 casos de prueba (3 sectores × 5 semillas, `refinanciacion` y `riesgo_refinanciacion`), rompiendo el test ya aprobado en el cuarto lote que confirma que leasing/obligaciones/otros mantienen su reparto propio. Revertido a la fórmula anterior (deudas_fin_largo/deuda_financiera_total) — el hallazgo de #71 sigue documentado y sin corregir, ahora con esta vía descartada explícitamente. **Pendiente de decisión conjunta** sobre cómo recalibrar (p. ej. una escala de intensidad propia y mucho más pequeña para 8/16, mismo patrón que la recalibración de #20 para 9/10/11, o acotar el objetivo a un rango estructuralmente alcanzable en vez de al huber±3·MAD genérico) antes de intentarlo de nuevo. **Efecto sobre el hallazgo mayor de #73**: al no haber podido corregir `calidad_deuda`, la posible contaminación de sus señales por el desajuste de fórmula SIGUE presente — no se puede descartar como explicación parcial de su tasa de señalización todavía. **Actualización tras repetir el stress test completo con `rotacion_existencias` ya corregido**: las cifras globales son prácticamente IDÉNTICAS a las de #73 (99,3% casos con señal, hallazgo original 21,54% exacto, overlap `riesgo_plausibilidad_pyg` 78,76% exacto, total >8 desviaciones 56.815 frente a 56.826 — variación de 11 casos, ruido) — confirma que el desajuste de fórmula de `rotacion_existencias` NO era un contribuyente relevante del hallazgo mayor, consistente con el diagnóstico de #75 (la causa dominante es acumulación entre años, no un desajuste de fórmula). Overlap con `riesgo_endeudamiento` mejora ligeramente (99,96% frente a 99,92%: 17 discrepancias frente a 31) — aparecen 4 discrepancias NUEVAS en archetypos que antes no las mostraban (`crecimiento_destruccion_caja`/`aumento_nof` en solitario, intensidad fuerte: el cambio de trayectoria de existencias desplaza algunos casos a través del umbral), verificadas una a una: las 4 son `contencion_al_limite=True` con `deterioro_aplicado_eur>0`, exactamente la excepción ya documentada en #73 — no es una excepción nueva | `motor/evolucion_arquetipo.py::_masa_circulante_objetivo`; suite completa (635 tests, sin modificar ningún valor de referencia); stress test completo repetido (`stress_plausibilidad.py`) |
| 75 | Diagnóstico (NO arreglo) del hallazgo mayor de #73: la cola extrema (>8 desviaciones) y la tasa de señalización alta sin ningún arquetipo empujando la magnitud comprobada, ¿vienen de combinar primitivas independientes DENTRO de un mismo año, o de la ACUMULACIÓN del ruido entre 2023→2024→2025? Mismo tipo de análisis que aisló la causa del arquetipo 4 (#19) | **Predominantemente acumulación entre años — no combinación dentro de un año.** Descompuesto por año, con arquetipos que NO tocan la magnitud comprobada (para aislar el ruido de la base de cualquier empuje real): `ratios.liquidez` bajo `apalancamiento:leve` (no toca circulante) — año 2023 (año base, un único sorteo, SIN posibilidad de acumulación entre años): 7,9% señalizado, desviación media 0,67; año 2024 (1 año de arrastre): 29,6%, media 2,49; año 2025 (2 años de arrastre): 55,6%, media 6,83, máximo 97,91. Mismo patrón en `ratios.tesoreria` y `ratios.fm_activo`. **Confirmado que NO depende del arquetipo activo**: el mismo crecimiento 2023→2024→2025 aparece IDÉNTICO bajo `resultado_extraordinario:leve` (que no toca circulante NI deuda/PN, solo una línea puntual de PyG en un año) — descarta que sea la "espiral de deuda" de `apalancamiento` (ya documentada, #9) filtrándose por otra vía; es un fenómeno general de la generación de base, independiente de cualquier arquetipo. **Mecanismo identificado** (caso concreto depurado, sector 24.1 grandes_medianas semilla 0): para una masa de circulante NO tocada por ningún arquetipo, `existencias`/`realizable`/`acreedores_comerciales` SÍ crecen de forma puramente proporcional a ventas (sin ruido nuevo cada año) — el arrastre entra por otra vía: el plug de cuadre (`ajuste_cuadre_eur`, en `_construir_balance`) absorbe la diferencia entre el patrimonio neto REAL de cada año (que sí lleva ruido independiente nuevo cada año, vía las primitivas de PyG) y el crecimiento "proporcional" que el resto del balance asume — y esa diferencia se resta de `otras_deudas_corto` (o, si no basta, de `disponible`). Ejemplo verificado: PN pasa de 4,08M€ (2023) a 5,65M€ (2024, +38,7%, muy por encima del crecimiento de ventas del caso) a 6,65M€ (2025); el plug correspondiente (-1,49M€ en 2024, -0,87M€ en 2025) reduce `otras_deudas_corto` de 2,45M€ a 1,01M€ a 0,16M€ — encogiendo `pasivo_corriente` cada año y empujando `liquidez` sistemáticamente al alza. Es un patrón tipo "paseo aleatorio": cada año suma una desviación de PN nueva e independiente sobre la ya acumulada de años anteriores, mientras el Huber/MAD del sector es un punto de referencia ESTÁTICO (empresas reales observadas en un momento dado, no una trayectoria simulada de 3 años) — la varianza simulada crece con cada año adicional, la real no. **Mismo tipo de causa que el ya corregido en su día para los sub-lotes de amortización** (hipótesis del usuario, confirmada). **No se ha tocado ningún código de generación en este encargo** — diagnóstico solamente, a la espera de decidir el abordaje (¿acotar el crecimiento del plug de cuadre año a año, atar el ruido de PyG a una banda más estrecha, otra vía?) | Script ad hoc (no un test permanente) — descomposición por año, `apalancamiento`/`resultado_extraordinario` a intensidad "leve", 27 sectores × 2 segmentos × 4 semillas |
| 76 | Recalibrar `reclasificacion_deuda` (8/16) con la fórmula ACCID de `calidad_deuda` (#74) usando una escala de intensidad propia y más pequeña, mismo patrón que #20 para 9/10/11 — NO acotar el objetivo a un rango arbitrario distinto de Huber±3·MAD, para no romper la coherencia del criterio de plausibilidad en todo el proyecto | **Calibración bloqueada — entrelazada con el hallazgo de #75, no completable de forma aislada.** Reimplanté la fórmula ACCID (#74) y probé varios candidatos de `INTENSIDAD_BASE_RECLASIFICACION_DEUDA` (0,005-0,03 en "leve", hasta 20-40× más pequeños que la escala genérica): la tasa de señal de `ratios.calidad_deuda` en "leve" se mantenía plana en ~33% para CUALQUIER candidato probado, señal de que la intensidad del arquetipo ya no era el factor dominante. Confirmado llevando la intensidad a ≈0 (arquetipo activo pero sin empuje real, mismo criterio que #18): la señal converge a 24,5% — prácticamente idéntica al 24,1% medido bajo `aumento_clientes:leve`, un arquetipo que no toca `calidad_deuda` en absoluto. **`ratios.calidad_deuda` ya tiene un suelo estructural de ruido de base (~24-25% en 2025) que es exactamente el mismo fenómeno del hallazgo mayor de #73/#75** (acumulación de ruido entre años vía el plug de cuadre, no el archetipo) — ninguna escala de intensidad, por pequeña que sea, puede bajar "leve" de ese suelo, igual que ninguna escala pudo bajar el suelo del 6,9% de `mejora_ebitda` en #18. Calibrar ahora produciría una tabla antes/después engañosa (el suelo se atribuiría a "mala calibración de 8/16" cuando en realidad es el hallazgo de #75 sin resolver). **Revertido de nuevo a la fórmula anterior** (deudas_fin_largo/deuda_financiera_total) — ni la fórmula ACCID ni la recalibración quedan aplicadas; ambas pendientes de retomar juntas una vez decidido el abordaje de #75, momento en el que la calibración de "leve" sí será representativa | Script ad hoc (no permanente) — barrido de candidatos e intensidad≈0, 27 sectores × 2 segmentos × 4 semillas; suite completa (635 tests, sin cambios de comportamiento: revertido al estado de antes de #74) |
| 77 | Diagnóstico de precisión sobre #75 (pedido explícito antes de proponer cualquier arreglo): ¿el fallo de diseño es "el PN varía de más" (ruido nuevo e independiente cada año) o "el resto del balance varía de menos" (cero variabilidad en años sin arquetipo)? | **Predominantemente "el PN varía de más" — confirmado por el código, no solo por inferencia.** `_generar_pyg_hasta_baii` (`motor/empresa_base.py`, reutilizada por `evolucion_arquetipo.py` cada año) sortea de nuevo, con `_generar_partida` y ruido típico/atípico COMPLETO contra el Huber/MAD del sector, cada primitiva de PyG no forzada por un arquetipo (`consumos_explotacion_pct`, `gastos_personal_pct`, `otros_gastos_explot_pct`, `resultado_extraordinario_pct`, `ingresos_financieros_pct`, `impuesto_beneficios_pct`, `tipo_interes`) — un sorteo INDEPENDIENTE de 2023, sin ligar al valor del año anterior, en CADA uno de los 3 años. El Huber/MAD del catálogo es una medida de variación TRANSVERSAL (entre empresas distintas del sector en un momento dado); redibujar así cada año trata cada ejercicio simulado como si fuera una empresa distinta sorteada de cero, no la evolución natural de la MISMA empresa — de ahí que el patrimonio neto (que absorbe el resultado de esas primitivas) acumule una desviación nueva e independiente cada año en vez de una desviación pequeña y correlacionada con la del año anterior, como sería realista para una empresa concreta. Contraste directo: las masas de circulante NO tocadas por ningún arquetipo (`existencias`/`realizable`/`acreedores_comerciales`) usan el patrón OPUESTO — perfil fijo desde 2023, sin ningún sorteo nuevo, solo escalado por ventas — que es el criterio ya establecido y consistente en el resto del proyecto ("perfil fijo desde 2023, desglose recalculado cada año"). **Diagnóstico secundario, más pequeño**: incluso el año base (2023, un único sorteo, sin acumulación posible) muestra una tasa de señalización ya algo elevada (7,9%-10,2% en liquidez/tesorería, frente a un ~1-3% que cabría esperar de un chequeo de 3·MAD bien calibrado) — sugiere que TAMBIÉN hay un componente menor de combinación de primitivas independientes dentro de un mismo año, pero es 3-8× más pequeño que el efecto de acumulación entre años (que multiplica la tasa hasta 55-61% en 2025) y no se ha aislado más allá de esta cifra. **Conclusión para decidir el alcance del arreglo**: la causa dominante y mejor evidenciada está en el lado del PN/PyG (redibujar completo cada año en vez de anclar al año anterior), no en el resto del balance (que ya sigue el patrón "correcto" usado en el resto del proyecto) — el arreglo debería tocar `_generar_pyg_hasta_baii`/el mecanismo de sorteo anual de PyG, no las masas de circulante. No descarto que el componente secundario (dentro de un año) merezca também alguna atención, pero es de un orden de magnitud menor. Sin implementar nada — solo el diagnóstico pedido | Código: `motor/empresa_base.py::_generar_pyg_hasta_baii` (líneas 926-1024), `motor/evolucion_arquetipo.py` líneas ~1990 (invocación anual); script ad hoc para la comparación año-base vs. resto |
| 78 | Implementar el diseño de #77 (año base sin cambios, `_generar_partida_con_memoria` para 2024/2025 incluido `tipo_interes`), calibrar `peso_memoria`/`factor_reduccion_ruido` con el método de siempre y traer la tabla antes/después | **Implementado y calibrado (`PESO_MEMORIA_PYG_ANUAL=0,6`, `FACTOR_REDUCCION_RUIDO_PYG_ANUAL=0,3`) — pero con un hallazgo crítico que limita mucho su alcance real, encontrado durante la propia calibración, NO decidido de antemano.** Nueva función `_generar_partida_con_memoria` (`motor/ruido.py`) — reversión a la media (AR(1): centro = peso_memoria·valor_anterior + (1-peso_memoria)·huber) con escala reducida (mad·factor_reduccion_ruido) y el mismo techo/suelo blando (±3·MAD) que el resto del motor; `_generar_pyg_hasta_baii` la usa para las 8 primitivas no forzadas + tipo_interes cuando se pasa `anterior_pyg_pct`/`anterior_tipo_interes` (solo en 2024/2025, `generar_empresa_base` no los pasa). **Tabla antes/después (27 sectores × 2 segmentos × 4 semillas, arquetipo "leve" que NO toca la magnitud comprobada):** `pyg.baii_pct`/`pyg.margen_bruto_pct` bajo `aumento_clientes` — 2023 24,5% (sin cambios, año base) → 2024 21,3%→18,1% → 2025 16,7%→10,6% (mejora real pero MODESTA: estos ratios YA eran planos/decrecientes antes del arreglo, no mostraban el patrón de crecimiento). `ratios.liquidez`/`tesoreria`/`fm_activo` bajo `resultado_extraordinario` — 2023 16,2% (sin cambios) → 2024 39,4%→37,0% → 2025 **65,7%→64,8%**: **prácticamente SIN CAMBIO**, pese a ser exactamente los ratios que mostraban el crecimiento 4× de #75/#77. **Hallazgo crítico**: llevando `peso_memoria`/`factor_reduccion_ruido` a valores extremos (0,98/0,02 — casi sin ruido nuevo en absoluto, primitivas casi "fijas desde 2023" como el resto del balance) la tasa de `fm_activo` en 2025 SOLO baja a ~49% (frente al ~65% original) — sigue muy lejos del ~13-16% del año base. **Depurado un caso concreto** (sector 24.1, `resultado_extraordinario:leve`, semilla 0, con memoria extrema activa): ventas crecen ~2%/año pero el patrimonio neto crece 4,08M€→4,57M€(+12,1%)→5,29M€(+15,8%) — el propio `resultado_ejercicio` se retiene ÍNTEGRO cada año (`patrimonio_neto = anterior + resultado_ejercicio`, sin dividendos ni distribución modelada, salvo la excepción explícita de apalancamiento) mientras el resto del balance (existencias/realizable/acreedores/otras_deudas) crece SOLO proporcional a ventas — un desajuste ESTRUCTURAL y DETERMINISTA (nada que ver con ruido aleatorio) entre la tasa de crecimiento del PN (≈ROE del caso, típicamente 8-16%/año) y la tasa de crecimiento del resto del balance (≈1-4%/año). El plug de cuadre absorbe esa brecha cada año encogiendo `otras_deudas_corto` — el mecanismo ya diagnosticado en #75, pero la CAUSA dominante de la brecha no es el sorteo con ruido de PyG (que #77 identificó y este encargo ya corrigió), es la retención del 100% del beneficio sin ningún mecanismo que la frene o la compense. **Esto significa que #77 estaba incompleto**: acertó en identificar el mecanismo de propagación (PN → plug de cuadre → `otras_deudas_corto` → ratios de circulante) y en que había SÍ un componente de ruido real (confirmado y corregido aquí, con mejora medible en baii/margen_bruto), pero se equivocó en la magnitud relativa — el ruido resultó ser una causa MENOR, no la dominante, para el lado de circulante/liquidez, que es precisamente el que domina el hallazgo mayor de #73 (la mayoría de los ratios con tasa alta — liquidez, tesorería, disponibilidad, fm_activo, fm_ventas — son de este tipo, no de tipo PyG). **Comiteado** (13da6c8) — implementación real y funcional, calibrada, con regresión completa en verde (635 tests: 1 test re-pineado — `test_reimplementacion_generica_reproduce_los_valores_de_referencia`—, 1 ampliado con una exclusión ya establecida — grupo89/subvención en el techo de `mejora_ebitda`—, 1 con el barrido de semillas ampliado por el mismo motivo que otros cambios de RNG en el proyecto). Decisión explícita del usuario: comitear ahora como mejora parcial y bien documentada, NO esperar a resolver el hallazgo mayor (que es su propio encargo, ver #79) | `motor/ruido.py::_generar_partida_con_memoria`; `motor/empresa_base.py::_generar_pyg_hasta_baii` (parámetros `anterior_pyg_pct`/`anterior_tipo_interes`); `motor/evolucion_arquetipo.py` (llamada en `_evolucionar_un_año`); suite completa (635 tests) |
| 79 | Diagnóstico (NO implementación) del mecanismo de retención/distribución de beneficios — causa dominante real del hallazgo mayor (#78): ¿qué tasa de reparto de dividendos sería necesaria, en términos generales, para que el crecimiento del PN se acompase con el del resto del balance? ¿Tiene el catálogo ACCID algún dato real de payout/reparto por sector para anclar esto, en vez de inventar un número? | **El catálogo/PDFs ACCID NO tienen ningún dato de payout/dividendos.** Verificado exhaustivamente: ni la CSV del catálogo (218 columnas, ninguna de dividendos/reparto) ni los 3 PDF fuente (`ratios2018/2021/2024.pdf`, ~382 páginas cada uno) mencionan "dividendo"/"payout"/"reparto de beneficio" en ningún sitio — la única coincidencia de "autofinanciación" es el título repetido de la sección 4.5 ("Ratios de rentabilidad y capacidad de autofinanciación"), que es la capacidad de autofinanciación YA capturada en el catálogo como `ratios.flujo_caja_activo`/`flujo_caja_ventas` — no dividendos. **Payout necesario, derivado matemáticamente de datos ACCID reales que SÍ existen** (no inventado): si `payout` es la fracción del resultado distribuida, `PN_crece = ROE·(1-payout)`; igualando al crecimiento de ventas `g` (el que ya usa el resto del balance, `RANGO_CRECIMIENTO_ORGANICO=[1%,4%]`, sorteado uniforme una vez por caso): `payout_necesario = 1 - g/ROE`. Con `ratios.roe.huber_9y` real de los 27 sectores del catálogo y `g=2,5%` (punto medio del rango): mediana 75,6% (P25 69,6%, P75 81,1%), rango de 23 a -16,8% (sector 68, "Actividades inmobiliarias", ROE=2,14% — MENOR que incluso el crecimiento mínimo de ventas: ni reteniendo el 100% del beneficio el PN llegaría a crecer tan rápido como ventas para ese sector, "payout negativo" no tiene sentido económico, sería necesaria una aportación de capital, no un dividendo). Con `g=1%` (mínimo del rango) la mediana sube a ~89% payout; con `g=4%` (máximo) baja a ~62%, y 2 sectores (68, 30.2) ya no alcanzan ni con 0% de payout. **Conclusión clave para el diseño**: NO existe una tasa de payout única y razonable que sirva para todos los sectores — el ROE real (y por tanto el payout necesario) varía por un factor de ~10× entre sectores (2,1% a 23%), así que una constante global fija sobre-corregiría a los sectores de ROE alto y sub-corregiría (o invertiría el problema) en los de ROE bajo. Cualquier mecanismo tendría que ser sensible al ROE/crecimiento de CADA caso concreto (p. ej. `payout = f(ROE_del_caso, g_del_caso)`), no una constante única — más parecido en espíritu a cómo `reclasificacion_deuda`/`masa_circulante` anclan contra el Huber del propio sector que a una intensidad fija tipo `INTENSIDAD_BASE`. Sin implementar nada — solo el diagnóstico y la comprobación de datos pedidos | Script ad hoc (no permanente); búsqueda de texto en los 3 PDF (`fitz`/PyMuPDF, mismo método que la verificación de fórmulas de `coste_deuda`/ratios2024); cálculo sobre `ratios.roe.huber_9y` de los 27 sectores |
| 80 | Implementar el mecanismo de payout de #79, con 3 puntos resueltos antes de tocar código: (1) fórmula por caso con variabilidad anclada al catálogo, no una tasa fija por sector; (2) techo/suelo razonables, residuo documentado como limitación, sin forzar una aportación de capital; (3) verificar cómo convive con el dividendo de apalancamiento (¿misma línea C.11.a o sub-línea propia?) ANTES de implementar, y confirmar con un combo explícito que no hay conflicto | **Implementado y verificado — mejora sustancial confirmada, no marginal.** `payout_caso` sorteado UNA vez por caso (mismo criterio que `capital_social`/`ventas_por_empleado` — rasgo estructural, no redibujado cada año, para no reintroducir el tipo de ruido que #78 corrigió), con `rng_tendencia` justo después de `crecimiento_pleno_objetivo`: `ROE_caso ~ _generar_partida(huber=ratios.roe.huber_9y, mad=ratios.roe.huber_scale_mad)` (mismo ruido mixto de siempre, sin dispersión inventada), `payout_caso = clamp(1 - g_caso/ROE_caso, 0, TECHO_PAYOUT_DIVIDENDOS=1.0)`. El techo (100%) es una cota matemática que nunca se alcanza (con g>0 y ROE>0, `1-g/ROE<1` siempre); el suelo (0%, sin efecto del mecanismo) sí se activa para sectores/sorteos de ROE bajo — documentado como limitación esperada, no forzada, mismo criterio que el aviso de fiabilidad del sector 30.3. `EjercicioEmpresa.payout_dividendos_eur` nuevo campo (`motor/evolucion_arquetipo.py`, dentro de `_evaluar`): `min(max(0, resultado_ejercicio)·payout_caso, disponible_eur)` — nunca sobre pérdidas, nunca más de lo que hay en caja (mismo criterio defensivo que `TECHO_FRACCION_DISPONIBLE_PRESTAMO_MATRIZ`). Resta de PN y de `disponible` (caja real, a diferencia del dividendo de apalancamiento, financiado con deuda nueva sin impacto de caja). **Convivencia con apalancamiento — verificada en código antes de decidir, no asumida**: `apalancamiento_extra_eur` es un campo propio y aislado, usado en solo 2 sitios (`efe.py:196`, `ecpn.py:189`), ninguno hardcodeado de forma peligrosa — se pudo sumar limpiamente. Decisión: **misma línea, sumados** (`c11a_dividendos = -(apalancamiento_extra_eur + payout_dividendos_eur)` en EFE; `operaciones_con_socios.reservas` análogo en ECPN) — el modelo oficial PGC solo tiene una línea de "Dividendos", sin distinguir fuente de financiación, y el cuadre no se rompe porque C.10 (deuda) solo refleja la parte de apalancamiento (con impacto neto cero en caja, ya cancelado ahí) mientras `disponible` ya absorbió el efecto real del payout por su lado. **Verificación de no-conflicto** (27 sectores × 4 semillas, `apalancamiento:fuerte` + payout de fondo, 216 ejercicios 2024/2025): 211 con `apalancamiento_extra_eur>0`, 184 con `payout_dividendos_eur>0`, 183 con AMBOS a la vez — 0 descuadres de balance, 0 de EFE, 0 de ECPN; `c11a_dividendos` coincide exactamente con `-(a+p)` en los 216/216; comprobado además que ambos importes son independientemente no-triviales y de magnitud comparable en los casos combinados (ni se pisan ni se anulan). **Verificación de plausibilidad (mismo método que #78 — controles limpios que no tocan PyG/circulante)**: `ratios.liquidez`/`tesoreria`/`fm_activo` bajo `resultado_extraordinario:leve`, 2025 — 65,7% (original) → 64,8% (#78, ruido de PyG) → **39,8% (con payout)**: una caída de 25 puntos porcentuales, sustancial, no marginal — aunque no vuelve al ~16% del año base. **Mejora real y validada, con un residuo cuya causa NO es el suelo de payout=0% en sectores de ROE bajo — descartado cuantitativamente, no solo por inspección** (correlación ROE_huber↔tasa de señal = 0,01; fracción de casos con payout=0↔tasa de señal = -0,09; los 17 sectores donde el payout está SIEMPRE activo tienen una tasa media de señal MAYOR — 44,1% — que los 10 sectores donde a veces se clampa a 0 — 32,5%). Ver #81 para el detalle completo — hallazgo abierto, investigación pendiente de la siguiente sesión. `pyg.baii_pct`/`margen_bruto_pct` (control `aumento_clientes:leve`) sin cambio relevante frente a #78 (10,2% frente a 10,6% — el payout no debía tocar el lado de PyG, y no lo hace). **Stress test completo repetido** (27 sectores × 2 segmentos × 4 semillas × 60 configuraciones, 12.960 casos, mismo script de #73/#78): casos con al menos 1 señal 99,3%→97,9%; `ratios.liquidez` 59,5%→40,9%, `ratios.tesoreria` 57,7%→39,4%, `ratios.fm_activo` 48,6%→30,8%, `ratios.disponibilidad_ratio` 51,0%→45,1% — mejoras sustanciales en los ratios de circulante, la familia que #78 apenas tocó. Hallazgo original (baii/margen, proxy "leve" de los 17 arquetipos) 21,54%→18,56%. Total de señales >8 desviaciones: 56.826 (original) → 56.815 (#78) → **48.601** (con payout), una reducción real del 14,5%. Las nuevas discrepancias de `riesgo_endeudamiento` que aparecen (sectores/semillas distintos a los de #73/#78, esperable: el sorteo nuevo de `payout_caso`/`ROE_caso` desplaza la secuencia de `rng_tendencia`) se verificaron una a una — encajan en la MISMA excepción ya documentada y con test de regresión desde #73 (`contencion_al_limite=True` con `deterioro_aplicado_eur>0`), no es una excepción nueva. Suite completa: 635 tests verdes (2 tests actualizados para reflejar el nuevo mecanismo, ninguno re-pineado con valores exactos: `test_patrimonio_neto_sigue_el_resultado_del_ejercicio` y `test_efe_apalancamiento_genera_flujo_bruto_de_financiacion`) | `motor/evolucion_arquetipo.py` (`payout_caso`, `EjercicioEmpresa.payout_dividendos_eur`, bloque de `_evaluar`); `motor/efe.py::generar_efe` (`c11a`); `motor/ecpn.py::generar_ecpn` (`operaciones_con_socios`); `tests/test_evolucion_arquetipo.py`, `tests/test_efe.py`; script ad hoc para las 2 verificaciones cuantificadas |
| 81 | Antes de cerrar #80 como "límite aceptado": ¿el residuo del 39,8% se concentra mayoritariamente en los sectores/casos donde payout=0% (el suelo ya conocido), o está repartido de forma pareja entre sectores — lo que significaría que queda algo más por entender? | **HALLAZGO ABIERTO — no es el suelo de payout=0%, descartado cuantitativamente. Causa real NO investigada todavía, pendiente de la siguiente sesión.** Medido `ratios.liquidez`/`tesoreria`/`fm_activo` en 2025 por sector (`resultado_extraordinario:leve`, 2 segmentos × 4 semillas = 8 casos/sector, 27 sectores), cruzado con `ratios.roe.huber_9y` del sector y la fracción de esos 8 casos con `payout_dividendos_eur≈0`:

| Sector | ROE huber | Señal 2025 | payout≈0 |
|---|---|---|---|
| 68 | 2,14% | 25,0% | 50,0% |
| 30.2 | 3,60% | 50,0% | 62,5% |
| 30.3 | 5,22% | 37,5% | 87,5% |
| 56.1 | 6,68% | 50,0% | 12,5% |
| 41.2 | 6,75% | 12,5% | 50,0% |
| 86.1 | 8,13% | 25,0% | 25,0% |
| 24.1 | 8,22% | 50,0% | 37,5% |
| 62 | 8,40% | 50,0% | 0,0% |
| 10.1 | 8,67% | 75,0% | 0,0% |
| 33.2 | 9,01% | 12,5% | 12,5% |
| 55.1 | 9,34% | 37,5% | 0,0% |
| 70.2 | 9,38% | 50,0% | 0,0% |
| 29 | 10,13% | 0,0% | 0,0% |
| 69.2 | 10,25% | 37,5% | 0,0% |
| 47.1 | 10,78% | 25,0% | 0,0% |
| 4941 | 11,50% | 37,5% | 0,0% |
| 28 | 11,56% | 87,5% | 0,0% |
| 35.1 | 11,89% | 75,0% | 0,0% |
| 20.1 | 12,00% | 100,0% | 0,0% |
| 52 | 12,14% | 25,0% | 0,0% |
| 33.1 | 13,25% | 0,0% | 0,0% |
| 71 | 14,80% | 75,0% | 0,0% |
| 46 | 15,28% | 25,0% | 0,0% |
| 19 | 17,17% | 0,0% | 12,5% |
| 43.2 | 17,44% | 25,0% | 0,0% |
| 85 | 18,25% | 25,0% | 0,0% |
| 72 | 23,00% | 62,5% | 37,5% |

Correlación (Pearson, 27 sectores): ROE_huber↔tasa de señal = **0,01** (esencialmente nula). Fracción payout≈0↔tasa de señal = **-0,09** (nula, y con el signo CONTRARIO al que predeciría la hipótesis del suelo). Separado en 2 grupos: los 17 sectores donde el payout NUNCA se clampa a 0 (mecanismo siempre activo con fuerza) tienen una tasa media de señal del **44,1%** — MAYOR que los 10 sectores donde a veces cae a 0 (**32,5%**). **Conclusión con el propio criterio del encargo**: el residuo NO está concentrado en los sectores de ROE bajo/payout=0 — está repartido de forma pareja (si acaso, algo más presente donde el mecanismo SÍ actúa con fuerza) — así que #80 no se cierra como "límite aceptado", hay una causa adicional sin identificar. **Deliberadamente sin especular sobre esa causa aquí** — queda para investigar en la siguiente sesión de trabajo, no en esta | Script ad hoc (no permanente) — 27 sectores × 2 segmentos × 4 semillas, `resultado_extraordinario:leve`, correlación de Pearson calculada a mano (sin librería externa) |
| 82 | Diagnóstico desde cero del residuo de #81 (mismo método que #77/#78 — descomposición del caso concreto, sin asumir "más de lo mismo"), con 4 causas posibles a valorar: (1) descomponer un caso trazado; (2) verificar si el PN crece a ritmo de `g` en la práctica, no solo en la fórmula; (3) descartar que algún lote granular reintroduzca ruido año a año; (4) valorar si el propio criterio Huber±3·MAD es demasiado estricto para una trayectoria simulada de 3 años | **4 causas encontradas — ninguna es "el mismo hallazgo, menor". La dominante y cuantificada es la (4).** **(0, metodológico)** El control `resultado_extraordinario:leve` usado en #75/#77/#78/#81 estaba parcialmente contaminado: el propio arquetipo 12 inyecta un `resultado_extraordinario` real en UN año, por diseño (no "no toca PN" como se asumió) — caso trazado sector 20.1 semilla 0: `resultado_ejercicio` +59% de 2024 a 2025, de los cuales 140.680€ son el suceso puntual. Repetido con `aumento_clientes:leve` (control genuinamente limpio): la tasa baja de 39,8% a 36,1% — real pero pequeño, no la causa dominante. **(2)** El payout SÍ funciona correctamente en la fórmula (verificado: `payout_caso` constante dentro de un caso, aplicado proporcionalmente bien) — pero el tope defensivo `min(payout, disponible_eur)` (añadido en #80 para no dejar caja negativa) SÍ se activa en la práctica: 3,2% de los ejercicios (barrido 27×2×4, control limpio), y en esos casos la tasa de señal sube a 57,1% (frente a 29,2% sin tope) — real, el doble de probable, pero solo explica ~6% de los casos señalizados. **(3) Descartado**: `arrendamiento_financiero` crece proporcional al total en el caso trazado (perfil fijo, no redibujado); tasa de señal con provisión activa (35,9%) vs. sin ella (36,2%) — sin diferencia, sin correlación. Los 4 lotes de desglose están limpios. **(4) — CAUSA DOMINANTE, cuantificada.** Con el ruido de PyG (#78) y la retención (#80) ya corregidos, queda un residuo PEQUEÑO pero real: en un caso limpio no capado, el PN crece 1,7-2,1%/año frente a un `g` de 1,01%/año (no las decenas de puntos de antes) — pero el criterio Huber±3·MAD, calibrado sobre variación TRANSVERSAL (entre empresas reales de un sector en un momento dado), sigue penalizando ese residuo pequeño con fuerza en sectores de MAD estrecho: correlación entre `mad/huber` de `ratios.liquidez` y la tasa de señal residual = **-0,454** (moderada-fuerte) en el barrido de 27 sectores — el sector 20.1 (MAD/huber=2,05%, el más estrecho del catálogo, `n_años=9` — no es un problema de muestra como el 30.3) señaliza en el 100% de los casos | Casos trazados (sector 20.1, semillas 0/1) con descomposición completa balance/PyG por año; barridos de correlación 27×2×4 para cada una de las 4 causas — sin implementar nada, solo diagnóstico |
| 83 | Implementar la Opción B para el hallazgo dominante de #82 (de 3 opciones propuestas — umbral fijo más ancho por año; banda que crece con los años tipo "paseo aleatorio"; comparar variación año a año en vez de nivel absoluto, descartada por falta de dato longitudinal ACCID): `techo(k) = huber + N·mad·√(1+k·factor)`, `k=años desde el año base` (0 en 2023, sin cambio), `N=3,0` sin tocar, factor único calibrado para que 2025 se acerque a la tasa del año base | **Implementado y calibrado (`FACTOR_ACUMULACION_VARIANZA_PLAUSIBILIDAD=3,0`) — 2025 coincide EXACTO con la tasa del año base en el control limpio.** `_evaluar_plausibilidad_caso` calcula `mad_efectivo = mad·√(1+k·factor)` para TODOS los ratios excepto `ratios.endeudamiento` (excluido deliberadamente: su techo es el límite absoluto `TECHO_ENDEUDAMIENTO_MAXIMO_ABSOLUTO=0,85`, ya usado EXACTO por la contención existente — no una banda estadística que deba ensancharse con los años). `SeñalRatio.huber_scale_mad` sigue reportando el MAD original del catálogo (no el efectivo) — solo `desviaciones` refleja el ensanchamiento, para que el campo siga siendo interpretable como "el MAD real del sector". **Tabla antes/después** (control limpio `aumento_clientes:leve`, `ratios.liquidez`/`tesoreria`/`fm_activo`, 27×2×4):

| Año | Antes (#81) | Después (Opción B) |
|---|---|---|
| 2023 (sin cambio, k=0) | 16,2% | 16,2% |
| 2024 (k=1) | 24,1% | 8,3% |
| 2025 (k=2, objetivo) | 36,1% | **16,2%** (idéntico al año base) |

2024 queda más laxo de lo que necesitaría en solitario (un único factor no puede calibrar k=1 y k=2 de forma independiente — el encargo pidió priorizar 2025 explícitamente, documentado como limitación conocida de la forma funcional de un solo parámetro). **Stress test completo repetido** (27×2×4×60, 12.960 casos): casos con ≥1 señal 97,9%→94,5%; `liquidez` 40,9%→20,3%, `tesoreria` 39,4%→21,6%, `fm_activo` 30,8%→14,4%, `disponibilidad_ratio` 45,1%→18,0%, `calidad_deuda` 38,8%→14,4%. Hallazgo original (baii/margen, proxy "leve") 18,56%→**9,65%** — mucho más cerca ya del 6,9% original de #18. Señales >8 desviaciones: 48.601→**28.237** (-41,9%). **Nueva excepción de superconjunto esperada y verificada, no un fallo**: en 2024/2025, `riesgo_plausibilidad_pyg=True` (contención SIN ensanchar, techo antiguo) puede no tener señal nueva (banda YA ensanchada) — verificado explícitamente que el valor cae DENTRO de la banda ensanchada en cada caso encontrado (no excluido a ciegas); test actualizado para comprobarlo, no solo excluirlo. `riesgo_endeudamiento` sigue con superconjunto EXACTO (sin tocar, por diseño). Suite completa: 635 tests verdes, sin re-pinear ningún valor de referencia (el cambio es puramente del criterio de plausibilidad, no toca generación) | `motor/evolucion_arquetipo.py::_evaluar_plausibilidad_caso` (`mad_efectivo`, `FACTOR_ACUMULACION_VARIANZA_PLAUSIBILIDAD`); `tests/test_plausibilidad_caso.py` (test de superconjunto actualizado); script ad hoc para calibración y stress test completo |
| 84 | Corregir la causa (2) de #82: el tope `min(payout, disponible_eur)` deja el beneficio de más RETENIDO en PN (en vez de repartido) cuando la NOF ya consumió la caja disponible — justo en los años de circulante tenso, componiendo la distorsión que el payout debía corregir | **Corregido — el payout objetivo ya NO se recorta por falta de caja: el déficit se financia con deuda a corto NUEVA, mismo criterio que `_deficit_y_deuda_corto` ya usa para el déficit de NOF (no un mecanismo nuevo).** `payout_dividendos_eur` vuelve a ser siempre `max(0, resultado_ejercicio)·payout_caso` (sin el `min(...,disponible)` de #80); si `disponible_eur - payout_dividendos_eur < 0`, el faltante se suma a `deudas_fin_corto_eur` y `disponible` queda en 0 — igual que el mecanismo de NOF. Nuevo campo expuesto `EjercicioEmpresa.payout_deuda_extra_eur` (mismo patrón de transparencia que `deuda_extra_por_nof_eur`), para que cualquier test/depuración pueda aislar esta deuda de la que generan los propios arquetipos. **No retroalimenta `gastos_financieros` de ese mismo año** (la deuda extra empieza a devengar interés desde el año siguiente, vía `deuda_financiera_inicio_eur`) — misma aproximación ya aceptada para los ajustes post-hoc de grupo89/provisión. **4 tests rotos y corregidos** (todos por la misma razón: el payout ahora SÍ inyecta deuda a corto real en más casos, que hay que aislar antes de comprobar el efecto de OTRO mecanismo): `test_ecpn_operaciones_con_socios_solo_cuando_hay_apalancamiento` (renombrado — desde #80 ya no es "solo apalancamiento", el payout es transversal; ahora compara contra `-(apalancamiento_extra_eur+payout_dividendos_eur)`), `test_refinanciacion_no_altera_la_deuda_financiera_total`/`test_riesgo_refinanciacion_no_altera_la_deuda_financiera_total` (restan `payout_deuda_extra_eur` del total antes de comparar contra el crecimiento proporcional), `test_refinanciacion_sube_la_proporcion_a_largo_plazo` (aísla `payout_deuda_extra_eur` de `deudas_fin_corto` antes de calcular la proporción largo/total — sin aislarlo, la deuda a corto nueva del payout podía enmascarar la propia subida de proporción que reclasificación produce). **Resultado cuantificado**: en el control limpio (`aumento_clientes:leve`), el tope se activaba en 3,2%→**5,6%** de los ejercicios (más casos detectados, ya no ocultos por el recorte silencioso) — la tasa de señal en 2025 baja de 16,2% (Opción B sola) a **13,0%**, por DEBAJO ya del propio año base (16,2%). Stress test completo repetido: casos con ≥1 señal 94,5%→94,4%; `liquidez` 20,3%→19,5%, `tesoreria` 21,6%→20,6%, `fm_activo` 14,4%→13,4%, `disponibilidad_ratio` 18,0%→17,7%, `calidad_deuda` 14,4%→13,3%; señales >8 desviaciones 28.237→27.795. Mejora más pequeña que la Opción B (era una causa menor, ~6% de los casos), pero real y en la dirección correcta. Suite completa: 635 tests verdes | `motor/evolucion_arquetipo.py` (`_evaluar`, `payout_deuda_extra_eur`); `tests/test_ecpn.py`, `tests/test_arquetipos_generalizados.py`, `tests/test_arquetipos_lote3.py`; script ad hoc para la verificación cuantificada y el stress test completo |
| 85 | ¿Sigue siendo razonable la calibración de intensidad de los arquetipos 9/14, 10, 11 (#20) tras el ruido de PyG (#78), el payout (#80/#84) y la banda de plausibilidad creciente (#82-83)? Comprobar ANTES de asumir que hace falta recalibrar, mismo método que #20 (barrido 27×4, tasa de activación por intensidad) | **Comprobado — el gradiente sigue siendo razonable, NO hace falta recalibrar nada.** Medido `riesgo_endeudamiento` (9/14) y `riesgo_plausibilidad_pyg` (10/11) por intensidad, 27 sectores × 4 semillas × 2 años (2024/2025), ambos segmentos:

| Arquetipo | Segmento | Leve | Moderado | Fuerte | Leve #20 (referencia) |
|---|---|---|---|---|---|
| 9/14 apalancamiento | grandes_medianas | 1,4% | 12,5% | 55,6% | 2,8% |
| 9/14 apalancamiento | pequeñas | 2,8% | 21,3% | 65,3% | 2,8% |
| 10 mejora_ebitda | grandes_medianas | 3,2% | 11,1% | 34,7% | 8,8% |
| 10 mejora_ebitda | pequeñas | 3,2% | 15,7% | 51,9% | 8,8% |
| 11 mejora_margen | grandes_medianas | 2,8% | 15,7% | 56,5% | 2,8% |
| 11 mejora_margen | pequeñas | 2,3% | 28,7% | 71,3% | 2,8% |

Los 3 arquetipos mantienen el gradiente objetivo (leve 1-7%, moderado intermedio, fuerte alto sin saturar cerca del 100%) en ambos segmentos. `mejora_margen` (11) queda prácticamente IDÉNTICO a #20 (2,8%/15,7%/56,5%, sin cambio perceptible). `apalancamiento` (9/14) baja ligeramente en "leve" (1,4%-2,8% frente a 2,8%) pero sigue dentro del rango objetivo. `mejora_ebitda` (10) es el que más se mueve: "leve" baja de 8,8% a 3,2% — pero esto es una MEJORA, no un problema: el 8,8% de #20 ya estaba documentado como por ENCIMA del objetivo (~7%) por el suelo estructural del hallazgo #18 (ruido de base sin ningún arquetipo) — con #78/#82 ya corrigiendo buena parte de ese ruido de base, el suelo estructural bajó y "leve" cayó dentro del rango objetivo por primera vez, sin haber tocado la escala de intensidad en absoluto. Ningún arquetipo se acerca a la saturación en "fuerte" (máximo 71,3%, lejos del ~100%). **No se ha tocado `INTENSIDAD_BASE_APALANCAMIENTO`/`_MEJORA_MARGEN`/`_MEJORA_EBITDA`** — verificado que no hacía falta antes de tocar nada, tal como se pidió | Script ad hoc (no permanente) — mismo método que #20, 27 sectores × 4 semillas × 2 segmentos × 2 años |
| 86 | Implementar la provisión de insolvencias de clientes (cuenta 490 del PGC) pedida por el usuario: DETERIORO DE ACTIVO (resta de "Clientes"/`realizable`, a diferencia de las provisiones del subgrupo 14, pasivo), probabilidad de fondo independiente de cualquier arquetipo (para que aparezca también con el 6), magnitud anclada a `ratios.cobro_dias` (función de anclaje a decidir y documentar), boost si el arquetipo 4 o el 7 están activos (criterio a decidir y documentar), dotación/reversión en PyG mismo patrón que provisiones, movimiento anual expuesto para la futura capa de incidencias didácticas, identidad Balance↔PyG explícita (lección de #60/lote 3) | **Implementado en `motor/insolvencias.py` — ver CLAUDE.md, sección "Deterioro de valor de créditos por operaciones comerciales", para el diseño completo.** Anclaje: rampa lineal `factor_riesgo_cobro_dias` entre 60 y 150 días (extremos reales del catálogo: 9,6-186,3 días), probabilidad 20%-40% e importe 2%-10% de "Clientes" escalando con el mismo factor. Boost del 4 (`clase="cuantitativo"`, detectado directamente) y del 7 (`clase="memoria_pura"` — único arquetipo de esa clase que influye en la evolución NUMÉRICA, vía un booleano nuevo que `motor.memoria.generar_caso_combinado` pasa a `generar_evolucion_combinada`): +10pp de probabilidad cada uno, ×1,3 de magnitud si cualquiera está activo (no acumulable). **Diseño distintivo frente a provisiones, encontrado durante la implementación (no en el encargo original)**: la aplicación (baja definitiva del derecho de cobro) NO puede recuperar `realizable` — a diferencia del pasivo de provisiones, donde aplicación Y exceso liberan el pasivo por igual, aquí solo la reversión (exceso) libera `realizable` de verdad; la aplicación ya se reconoció como pérdida en PyG el año de la dotación y no debe "revertirse" solo por formalizarse. Resuelto con `insolvencia_deduccion_realizable_eur = importe_dotado_eur − exceso_acumulado_eur` (distinto de `insolvencia_saldo_eur`, que sí decrece con ambas, expuesto para la memoria/incidencia futura) — verificado con un test dedicado. La deducción se aplica SOLO dentro de `_evaluar`, después de `_deficit_y_deuda_corto` (que sigue viendo el `realizable` bruto, para no contaminar el cálculo de NOF con un evento que no es de ciclo de caja). **Verificado EN LA PRÁCTICA que el booleano del 7 llega de verdad y cambia el resultado** (pedido explícitamente, no solo a nivel de fórmula): sector de cobro corto (47.1, probabilidad de base en su suelo 20%), 40 semillas vía `generar_caso_combinado` — tasa de activación 20,0% sin el 7 → 27,5% con el 7; semilla 23 es un caso "flip" completo; en los casos donde ambos activan, el importe con el 7 escala EXACTAMENTE ×1,3000. **EFE sin líneas nuevas, verificado empíricamente** (no solo algebraico): `a3b_deudores` ya existente reconcilia exacto. **20 tests nuevos en `tests/test_insolvencias.py`, todos verdes** (probabilidad de fondo con el 6, anclaje a cobro_dias a nivel de fórmula y observado, boost de 4/7 a nivel de fórmula y observado incluido el caso "flip", movimiento anual, la propiedad aplicación-no-recupera-realizable, identidad Balance↔PyG explícita, suma exacta con Clientes/Deudores, EFE/ECPN sin líneas nuevas). **Regresión**: 25 instancias de test preexistentes (11 funciones, varias parametrizadas por sector/intensidad) rotas por la probabilidad de fondo (más alta que provisiones, 20%-40% vs. 25%, y sensible a `ratios.cobro_dias` — activa con más frecuencia en cualquier caso, no solo con arquetipos de riesgo) tocando `realizable`/`margen_bruto`/`baii` en tests que asumían esas magnitudes intactas sin el mecanismo — corregidos aislando explícitamente el efecto de insolvencias (mismo patrón que #39/#60/#84 para grupo89/provisión/payout), 0 re-pins de valores de referencia. Suite completa: 655 tests verdes | `motor/insolvencias.py` (nuevo); `motor/evolucion_arquetipo.py` (`EjercicioEmpresa`, `_evolucionar_un_año`, `_evaluar`, `generar_evolucion_combinada`); `motor/memoria.py` (`generar_caso_combinado`); `tests/test_insolvencias.py` (nuevo, 20 tests); `tests/test_arquetipos_generalizados.py`, `tests/test_arquetipos_lote3.py`, `tests/test_evolucion_arquetipo.py` (11 funciones de test aisladas del nuevo mecanismo) |
