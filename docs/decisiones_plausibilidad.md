# Decisiones de plausibilidad ya cerradas

Log de hallazgos ya investigados y corregidos (o aceptados con documentación) — atajo de arranque
de sesión, ver `CLAUDE.md` para las restricciones de uso. **No repetir la investigación ni volver
a dudar de algo ya verificado aquí**; si hace falta el detalle cuantitativo completo, está en el
docstring de `motor/evolucion_arquetipo.py` (buscar la sección del arquetipo correspondiente) y en
los tests de regresión que lo bloquean.

| # | Hallazgo | Corrección / decisión | Test de regresión |
|---|---|---|---|
| 1 | Endeudamiento sin techo de plausibilidad sectorial | Chequeo incondicional cada año contra `huber+3·MAD`, techo absoluto 0,85 (bajado de 0,95 tras comprobar que 26/1.296 casos del arquetipo 9 se colaban con 0,95) | `test_apalancamiento_topa_la_deuda_nueva_al_techo_del_sector` |
| 2 | "Espiral de deuda" del arquetipo 9: el interés de deuda YA acumulada erosiona PN en años sin deuda nueva, sin quedar señalizado | El chequeo de endeudamiento se quitó de estar condicionado a `deuda_extra_por_nof_eur>0` — ahora corre siempre. Antes: 130/1.296 sin señalizar; después: 0 | `test_espiral_de_deuda_de_apalancamiento_siempre_queda_señalizada` |
| 3 | Techo propio de `consumos_explotacion` no acotaba el `margen_bruto` derivado (arquetipo 11) | `_limitar_por_subtotal`: topa el subtotal que alimenta la primitiva a su propio huber±3·MAD (`SUBTOTAL_PYG_DE_PRIMITIVA`). Antes: 1.796/2.592 (69%) sin señalizar; después: 0 | `test_margen_bruto_respeta_el_techo_de_plausibilidad_del_sector` |
| 4 | Dampear una masa de PASIVO (acreedores_comerciales) como palanca de contención tiene efecto neto CERO sobre endeudamiento | Palancas de contención restringidas a masas de ACTIVO (`SIGNO_NOF_MASA_CIRCULANTE == 1`) | Cubierto por los tests del arquetipo 4 en `test_arquetipos_generalizados.py` |
| 5 | Arquetipo 15: ¿tesorería puede quedar negativa/absurda sin señal? | No — el suelo genérico "disponible no puede ser negativo" del pipeline ya lo cubre; comprobado 0/6.480 casos | — (verificación puntual, no hubo fix) |
| 6 | Arquetipo 10 (baii, base dinámica): sin techo, `baii_pct` rebasaba el suyo sin señal | `_limitar_gastos_personal_por_baii`. Antes: 316/648 (48,8%) sin señalizar; después: 0 | `test_mejora_ebitda_baii_respeta_el_techo_de_plausibilidad_del_sector` |
| 7 | Arquetipo 10: la corrección del techo a veces invierte el sentido de `gastos_personal_pct` (sube en vez de bajar) | Se amortigua la intensidad del propio arquetipo antes de invertir el sentido; solo invierte cuando ni con intensidad cero bastaría (ruido de la base ese año). Señalizado con `pyg_contencion_al_limite`. 173/316 activaciones se resuelven sin invertir, 143/316 sí — las 143 coinciden EXACTAMENTE con la señal, 0 discrepancias en 648 casos | `test_mejora_ebitda_las_subidas_de_gastos_personal_siempre_coinciden_con_el_limite_por_ruido` |
| 8 | Sesgo de RNG compartido: `nota_memoria` del arquetipo 10 se sembraba con `rng_pyg`, que llega al punto del sorteo en el MISMO estado para cualquier sector/intensidad con la misma semilla | RNG dedicado `rngs_nota`, sembrado con hash estable (`zlib.crc32`) de sector+segmento+intensidad. Antes: reparto 12/37/56/33/5 en 143 casos (χ²≈58, p<0,001, NO es ruido de muestra); después: χ²=2,42 (uniforme) | Tests de `nota_memoria` en `test_arquetipos_generalizados.py` |
| 9 | Mismo sesgo, auditado y encontrado también en `generar_empresa_base` (año base 2023) y en `semilla_secuencia`/`rng_tendencia`/`rngs_pyg` (2024/2025, TODOS los arquetipos) | Mismo patrón de hash estable sector+segmento en ambos puntos — **deliberadamente sin mezclar intensidad** (mismo "ruido de fondo" entre intensidades del mismo caso, por diseño). Antes: z-scores idénticos hasta 1e-9 entre sectores distintos con la misma semilla (27/27 duplicados); después: 0 duplicados en 27×4 combinaciones. Forzó a re-pinnear los valores exactos del test de regresión del arquetipo 1 (el mecanismo no cambió, solo el ruido de fondo que recibe) | `test_sectores_distintos_no_comparten_ruido_pese_a_la_misma_semilla` (empresa_base) y `test_sectores_distintos_no_comparten_crecimiento_pleno_objetivo` (evolución) |
| 10 | Arquetipo 14 (ROE elevado por apalancamiento): ¿el ROE realmente sube al declararlo como variante exacta del 9? | El ROE *headline* solo sale más alto que la base 2023 en el 53,3% de 324 casos. Se probó (e implementó, midió y REVIRTIÓ) amortiguar el ruido operativo de las primitivas no forzadas — descartado: ni con ruido cero mejora (49,7%). Causa real: la misma "espiral de deuda" del hallazgo #2 aplicada al ROE — sube 61,7% en la transición 2024 vs. 2023 (el interés nuevo pesa "media dosis" ese año), pero solo 35,0% en 2025 vs. 2024 (interés ya pesa el año completo + contención limita la deuda nueva). En AL MENOS uno de los dos años: 78,1%, consistente por intensidad (77-79%). **Decisión: no se toca el motor** — el 78,1% ya es claramente mayoritario y tocar la rentabilidad operativa confundiría dos huellas distintas; la ficha docente es la que debe matizarse (pendiente, fuera del motor) para reflejar que el efecto se ve típicamente en el año en que se contrae la deuda, no necesariamente en el último año del caso | — (verificación cuantificada, sin fix en el motor) |
| 11 | Arquetipo 17 (capex): financiado con deuda a largo nueva, sin palanca de circulante para contener endeudamiento | Aceptado, mismo patrón ya establecido para 9/11/15/16: el chequeo incondicional señaliza siempre (0 casos sin señalizar en 972), pero el 100% de las activaciones (152/972, 15,6%) queda en `contencion_al_limite=True` — señal honesta sin corrección posible, no un intento fallido | `test_capex_elevado_riesgo_endeudamiento_siempre_señalizado` |
| 12 | Arquetipo 18 (adquisición): salto exógeno de activo financiado con caja+deuda ¿puede producir combinaciones no plausibles? | Mismo patrón que #11: el chequeo incondicional se activa en 70/972 (7,2%), el 100% señalizado, el 100% en `contencion_al_limite=True`. Además: en 3/324 casos (sector con `disponible` alto y `otras_deudas_corto` bajo de partida) el drenaje de caja de la operación hizo que el plug general de cuadre (ya existente, no nuevo) redirigiera un remanente hacia `disponible` — el balance sigue cuadrando exacto, es una interacción del mecanismo de cuadre PRE-EXISTENTE más visible aquí por la magnitud del salto, no un bug del arquetipo | `test_adquisicion_riesgo_endeudamiento_siempre_señalizado`, `test_adquisicion_incremento_de_activo_proporcional_a_intensidad` |
| 13 | Arquetipo 18 (adquisición): la sección 2.24 pide "fondo de comercio, si procede" como parte de la huella — el modelo no lo desglosa como partida propia | **Limitación conocida del modelo de balance actual, no resuelta ahora, no un olvido.** `MASAS_BALANCE` (motor/empresa_base.py) no tiene una categoría "fondo_comercio": el incremento de activo del arquetipo 18 se registra íntegro dentro de `activo_no_corriente`, sin distinguir qué parte sería inmovilizado material/intangible identificable y qué parte fondo de comercio. Añadirla exigiría tocar `MASAS_BALANCE`, la generación del balance base, y cada consumidor del balance (endeudamiento, ratios, memoria) — desproporcionado para lo que pide hoy la sección 2.24, que no exige una cifra de fondo de comercio separada, solo mencionarlo en la memoria "si procede" (ya cubierto por `NOTAS_MEMORIA_ADQUISICION`). Revisitar si una fase futura (p. ej. estados financieros con más desglose, o un arquetipo que necesite distinguir explícitamente el fondo de comercio, como pruebas de deterioro) lo exige | — (limitación documentada, sin test — no hay comportamiento que fijar) |
| 14 | Arquetipos 20/21 (`motor/memoria.py`): el % puro (sin suelo) de la magnitud de referencia podía dar importes citados ridículamente pequeños para una "operación relevante" de memoria | Verificado en pruebas de estrés (324 casos): arquetipo 20 (operaciones vinculadas) sin suelo — mínimo 8.000 €, 1 caso <10.000 €, 5 <20.000 €, 17 <50.000 € de 324; arquetipo 21 (coberturas) salió limpio (mínimo 60.000 €, 0 <50.000 €) pero se le añadió un suelo igualmente por el mismo criterio defensivo. Corregido con `SUELO_IMPORTE_VINCULADAS_EUR` (30.000 €) y `SUELO_NOCIONAL_COBERTURA_EUR` (50.000 €) — el `%` mostrado en el texto se RECALCULA sobre el importe ya topado, nunca el % originalmente sorteado, para que texto e importe nunca queden internamente inconsistentes. Verificado tras el fix: 0 casos por debajo del suelo Y 0 por encima del techo (activo total) en los mismos 324 casos, para ambos arquetipos — confirmado también que el suelo nunca puede empujar el importe por encima de la magnitud de referencia (deuda financiera mínima observada 151.500 €, muy por encima de cualquiera de los dos suelos) | `test_operaciones_vinculadas_importe_dentro_de_cotas_de_plausibilidad`, `test_coberturas_nocional_dentro_de_cotas_de_plausibilidad` |
| 15 | Arquetipo 22 (comodín, tema "dependencia de un proveedor clave") se solapa temáticamente con el arquetipo 7 (dependencia de pocos clientes) — mismo riesgo de concentración, lado contrario de la relación comercial | **RESUELTO — ver #16.** Cuando se combinen varios arquetipos de memoria sobre el mismo caso, evitar que un caso combine el tema "proveedores"+"concentracion" del 22 con el 7 ("clientes"+"concentracion") — no son contradictorios, pero sí repetitivos (dos riesgos de concentración a la vez, de lados opuestos de la cadena de valor). Marcar como "temáticamente solapados" en esa lógica futura, no aquí — las etiquetas ya existen (`concentracion` aparece en ambos) para que esa fase no tenga que rehacer el etiquetado | `test_arquetipos_7_y_22_evitan_colision_de_tema_en_barrido_amplio` (`tests/test_combinacion_arquetipos.py`) |
| 16 | Combinación de arquetipos (sección 2.12, acotada a las 6 combinaciones recomendadas de la sección 2.26): (a) ¿el motor soporta varios arquetipos a la vez? — no, antes de este hallazgo: `arquetipo_id` era un string único en toda la cadena; (b) al combinarlos, ¿se necesitan reglas nuevas de composición, y con qué coste en saturación? | (a) Se generalizó `generar_evolucion_arquetipo` a `generar_evolucion_combinada` (acepta `dict[arquetipo_id, intensidad]`); el caso de 1 arquetipo pasó a ser el wrapper de una línea, garantía estructural de que la regresión de los 21 arquetipos cerrados prueba la ausencia de cambio de comportamiento (confirmado: suite completa, 0 valores de referencia modificados). (b) De las 4 colisiones sospechadas al diseñar, 2 NO eran colisiones reales (Combo C "tesorería" y Combo D "BAII" ya componían bien por el pipeline existente, verificado leyendo código antes de asumir) — la única real fue Combo F (1+5) sobre `existencias`, resuelta sumando intensidades y aplicando la fórmula de continuidad una vez (ver "Combinación de arquetipos" en `CLAUDE.md`). **Hallazgo de saturación (verificación pedida explícitamente antes de dar el combo por bueno):** en barrido de referencia (27 sectores × 4 semillas, intensidad fuerte), arquetipo 1 solo activa `riesgo_endeudamiento` en 17,1% de los ejercicios, arquetipo 5 solo en 4,2% — la combinación 1+5 lo activa en 48,6% (muy por encima del ~20,6% esperable bajo independencia estadística de ambos efectos), y el Combo F completo (1+5+9+16) en 84,3%, con `contencion_al_limite` en el 71,4% de esas activaciones. Es la consecuencia directa y esperada de sumar intensidades sobre la misma variable de `masa_circulante` (regla aprobada) más el apalancamiento (9) y riesgo de refinanciación (16) actuando a la vez sobre la misma empresa ya tensionada — no un bug (balance sigue cuadrando en el 100% de los casos, la señalización nunca falla), pero sí una saturación desproporcionada frente a la suma ingenua de sus partes. **RESUELTO sin amortiguador de combinación — ver #20.** Investigación posterior: antes de tocar el Combo F, se descubrió que la mayor parte de esta "saturación" no venía de la combinación en sí, sino de que el arquetipo 9 en solitario ya activaba `riesgo_endeudamiento` en el 39,4%/67,6%/85,6% de los casos (leve/moderado/fuerte) — el Combo F completo (39,8%/67,6%/84,3%) era prácticamente idéntico a "9 solo". Tras recalibrar 9/10/11 (#20), el Combo F queda en 6,0%/22,2%/63,9% (leve/moderado/fuerte) — gradiente real, sin saturación en leve — confirmando la sospecha: **el amortiguador de combinación para 3+ arquetipos nunca hizo falta; la causa real era la calibración de intensidad de 9 en solitario, ya corregida** | `tests/test_combinacion_arquetipos.py` (17 tests: 6 combos × 2 sectores, disjunción exhaustiva de etiquetas 18/19/20/21, colisión 7+22 en barrido de 324 casos, fusión de `masa_circulante` sin descuadres, tasa de `contencion_al_limite` del Combo F registrada) |
| 17 | `riesgo_endeudamiento` NO es el indicador correcto para evaluar la calibración de intensidad de los arquetipos 2 (beneficio_sin_cash_flow) y 15 (riesgo_liquidez_pese_beneficio) — ambos usan `EfectoTesoreria`, que reduce `disponible` mediante `factor = max(0, 1 + dirección×intensidad_efectiva)` (`_disponible_proporcional_con_efecto`) ANTES de que el pipeline de déficit/deuda entre en juego. Medida su tasa de activación de `riesgo_endeudamiento` en el barrido estándar (27 sectores × 4 semillas), sale plana en 0,9% en las tres intensidades — a primera vista parece "insensibilidad" a la intensidad, igual que el patrón de 16 | **No es una falta de calibración: es que `riesgo_endeudamiento` mide algo que este mecanismo, por construcción, casi nunca llega a disparar.** Como el `factor` nunca es negativo y ninguno de los dos arquetipos toca masa_circulante (no hay déficit de NOF que sumar), `disponible_bruto_eur = disponible_proporcional_eur` es prácticamente siempre ≥0 — la vía "tesorería insuficiente → deuda a corto nueva → sube endeudamiento" casi nunca se activa. El indicador correcto es el propio mecanismo del arquetipo: la caída de `disponible/ventas`. Medido en el mismo barrido (27 sectores × 4 semillas, ambos arquetipos idénticos por compartir mecanismo): **disponible/ventas medio 23,17% (leve) → 19,94% (moderado) → 16,50% (fuerte)** — caída real, monótona y de magnitud razonable (~29% relativa entre leve y fuerte); el caso mínimo observado también desciende de forma consistente (3,55%→2,64%→1,61%) sin llegar nunca a 0 (0/216 casos en las tres intensidades). **Conclusión: 2 y 15 SÍ tienen un gradiente de intensidad real y sensible — no forman parte de ningún problema de calibración pendiente.** Si en el futuro se quiere seguir usando `riesgo_endeudamiento`/`contencion_al_limite` como criterio uniforme de "arquetipo con contención de plausibilidad" en algún informe o test, hay que recordar que para 2/15 (y en menor medida 8/16, por la misma razón estructural: su mecanismo tampoco alimenta el pipeline de déficit) esa señal no es representativa — el criterio correcto varía según qué variable toca cada mecanismo | — (verificación cuantificada, sin fix — el mecanismo ya funciona como debe) |
| 19 | Arquetipo 4 (deterioro_ciclo_caja) activa `riesgo_endeudamiento` mucho menos que el 1 (crecimiento_destruccion_caja) — 0,9%/0,9%/1,4% vs. 1,4%/6,5%/17,1% (leve/moderado/fuerte) — pese a tener un footprint de presión NOF proporcionalmente razonable (~80-85% del de 1, confirmado midiendo la magnitud real, no solo la tasa: `exceso_realizable` prácticamente idéntico al de 1 en las tres intensidades). ¿Es un problema de calibración del arquetipo 4? | **No — comportamiento explicado, no se toca.** Forzando el mismo `rango_crecimiento_pleno` del arquetipo 1 sobre el 4 (para aislar el efecto del crecimiento de ventas propio de 1) el hueco se cierra del todo en "leve" pero solo parcialmente en moderado/fuerte (34%/50% del hueco). Rastreado euro a euro, año por año (2024/2025), la causa real es un **efecto de umbral no lineal del colchón de tesorería, compuesto con la "espiral de deuda" ya documentada en el hallazgo #2**: la presión NOF de 1 es solo ~1,7-3× la de 4 (no desproporcionada), pero `disponible` actúa como un colchón que hay que agotar del todo antes de que aparezca deuda nueva — la presión moderada de 4 queda a menudo absorbida ENTERA por el colchón (deuda extra = 0 en varios casos observados), mientras que la mayor presión de 1 agota el colchón y genera deuda nueva en 2024 — una diferencia lineal pequeña en la presión se convierte en una diferencia grande en si se genera deuda o no. Esa deuda de 2024 (cuando existe) genera interés en 2025 que erosiona el PN — la misma espiral de deuda del arquetipo 9 — y en varios casos observados 2025 dispara `riesgo_endeudamiento` con presión NOF incremental de ESE año en CERO, puramente por el interés heredado de 2024. Como 4 casi nunca tomó deuda en 2024, casi nunca arrastra esa espiral a 2025, y ahí se concentra el hueco. **Son dos mecanismos ya existentes y ya documentados (colchón de tesorería + espiral de deuda) interactuando de forma no lineal pero explicable con un footprint proporcionalmente correcto — no un fallo nuevo del arquetipo 4.** Alguien auditando esto en el futuro no necesita repetir la investigación: la tasa distinta de 4 frente a 1 es la consecuencia esperable de esta interacción, no una señal de descalibración | — (verificación cuantificada — rastreo euro a euro por año, 4 semillas — sin fix, sin test nuevo: no hay comportamiento que fijar) |
| 20 | Recalibración de intensidad de los arquetipos 9 (y 14, variante exacta), 11 y 10 (ver #16 y #18): su señal de plausibilidad saturaba ya en "leve" (39,4%/41,7%/30,6% respectivamente, ninguno bajaba de ~7% ni con intensidad-efectiva casi nula para el 10 — suelo estructural, ver #18), muy por encima del resto de arquetipos bien calibrados (~1-7% en leve) | **Escalas de intensidad específicas por mecanismo**, sustituyendo la `INTENSIDAD_BASE` genérica (0,15/0,30/0,50) SOLO para estos 3 arquetipos (`INTENSIDAD_BASE_POR_ARQUETIPO` en `evolucion_arquetipo.py`) — calibradas empíricamente con el mismo barrido de siempre (27 sectores × 4 semillas), propuestas en 2 candidatas por arquetipo y aprobadas por el usuario tras revisión: apalancamiento (9/14) `0,02/0,08/0,25`; mejora_margen (11) `0,02/0,07/0,22`; mejora_ebitda (10) `0,015/0,06/0,22`. Resultado tras el cambio: 9 y 14 → 2,8%/14,4%/59,7% (idénticos entre sí, por compartir mecanismo exacto); 11 → 2,8%/15,7%/56,5%; 10 → 8,8%/16,2%/38,4% (por encima del objetivo de ~7% en leve por el suelo estructural del #18, es el mínimo alcanzable). **No hizo falta re-pinear ningún test**: la suite completa (333 tests) sigue en verde sin modificar ni un solo valor de referencia — los tests que dependen de la frecuencia de activación de estos 3 arquetipos ya estaban escritos de forma tolerante (`comprobados > 0`, "0 casos sin señalizar", nunca un recuento exacto atado a la escala de intensidad), así que siguen siendo válidos con la nueva escala sin tocarlos. **Nota para hallazgos #1/#2/#3/#6/#7/#10 (todos sobre 9/10/11/14, medidos con la escala antigua):** la mecánica de contención en sí (techo de endeudamiento, `_limitar_por_subtotal`, `_limitar_gastos_personal_por_baii`, espiral de deuda) no se ha tocado — solo la frecuencia con la que se activa — así que sus conclusiones cualitativas ("0 casos sin señalizar", "la contención siempre coincide con el límite por ruido") se mantienen (confirmado por los mismos tests, todavía en verde), pero **las cifras de frecuencia citadas en esos hallazgos (26/1.296, 130/1.296, 1.796/2.592, 316/648, 173/316 vs. 143/316, 53,3%/78,1%) corresponden a la escala de intensidad ANTERIOR (0,15/0,30/0,50) y ya no son representativas de la frecuencia actual** — no se han vuelto a medir con la escala nueva por no ser necesario para ninguna garantía de comportamiento, pero cualquiera que las cite debe saber que son históricas, no del estado actual. Efecto colateral esperado y confirmado en `#16`: el Combo F, que antes saturaba porque el arquetipo 9 solo ya estaba en 39,4%/67,6%/85,6%, ahora parte de una base mucho más baja | Suite completa (333 tests, sin modificar); `tests/test_combinacion_arquetipos.py` para el efecto sobre las combinaciones |
| 18 | **Hallazgo pendiente, fuera de alcance del encargo actual de recalibración de 9/10/11 — abordar como su propio encargo futuro (validación de plausibilidad de la sección 2.13 sobre el caso completo, no por arquetipo).** Al investigar por qué el arquetipo 10 (mejora_ebitda) tiene un suelo estructural del 6,9% en `riesgo_plausibilidad_pyg` incluso con intensidad prácticamente nula, se confirmó que `motor/empresa_base.py` (generación del ejercicio 2023 SIN ningún arquetipo activo) no aplica ningún techo/suelo de plausibilidad sobre `baii_pct` ni `margen_bruto_pct` como subtotales — solo trunca cada primitiva individual por separado (`_generar_partida`/`_normal_truncada`, máx. 1,5σ típico / 3,0σ atípico frente al huber de ESA primitiva, `PROB_ATIPICO=0,15`). `baii` se calcula combinando 4 primitivas ya truncadas independientemente (`valor_añadido − gastos_personal − amortizaciones + resultado_extraordinario`) sin ningún chequeo sobre el subtotal resultante; lo mismo para `margen_bruto = ingresos_explotacion − consumos_explotacion`. El único chequeo de subtotal que existe (`_limitar_por_subtotal`, `_limitar_gastos_personal_por_baii`) vive dentro de `evolucion_arquetipo.py` y solo se activa cuando un arquetipo empuja esa primitiva concreta — **un caso SIN ningún arquetipo activo (o con uno que no toque esas primitivas) puede tener `baii_pct`/`margen_bruto_pct` implausible sin que nada lo detecte hoy.** Evidencia cuantificada: barrido de referencia (27 sectores × 4 semillas) con intensidad ≈0 (arquetipo activo pero sin empuje real) — `riesgo_plausibilidad_pyg` se activa en el 6,9% de los ejercicios, igual en las tres intensidades — puramente por el ruido de la base, no por ningún arquetipo | No corregido — el chequeo pedido (techo/suelo de `baii_pct`/`margen_bruto_pct` sobre el caso completo, independientemente de si hay arquetipo activo) es un alcance mayor que toca `empresa_base.py`, no solo `evolucion_arquetipo.py`, y probablemente conviene abordarlo junto con el resto de la validación de plausibilidad de la sección 2.13 sobre el caso ya cerrado (los 3 ejercicios juntos), no arquetipo a arquetipo | — (hallazgo documentado, sin test — pendiente de encargo futuro) |
